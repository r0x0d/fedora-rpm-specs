# Generated by go2rpm 1.6.0
%bcond_without check
%global debug_package %{nil}

# https://github.com/cnf/structhash
%global goipath         github.com/cnf/structhash
%global commit          e1b16c1ebc089a0ae23d05adbb58189ba3868ba7

%gometa

%global common_description %{expand:
Go (golang) package to get hash strings of arbitrary data structures.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Version:        0
Release:        %autorelease -p
Summary:        Go (golang) package to get hash strings of arbitrary data structures

License:        ISC
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg

%prep
%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
