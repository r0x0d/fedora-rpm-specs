# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/liquidweb/liquidweb-go
%global goipath         github.com/liquidweb/liquidweb-go
Version:                1.6.4

%gometa -L -f

%global common_description %{expand:
Golang API client for Liquid Web's Storm API.}

%global golicenses      COPYRIGHT LICENSE
%global godocs          examples README.md lib/legacy/readme.md

Name:           golang-github-liquidweb
Release:        %autorelease
Summary:        Golang API client for Liquid Web's Storm API

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
