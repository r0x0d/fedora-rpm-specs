# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate zmq-sys

Name:           rust-zmq-sys
Version:        0.11.0
Release:        %autorelease
Summary:        Low-level bindings to the zeromq library

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/zmq-sys
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * port from the obsolete metadeps to system-deps 6
# * drop feature for using vendored libzmq sources
Patch:          zmq-sys-fix-metadata.diff
Patch:          0001-port-from-metadeps-to-system-deps.patch
# * FIXME: hack the test suite so that it passes on 32-bit architectures
#   (a proper fix would be to run bindgen in the build script)
Patch:          rust-zmq-sys-0.11.0-test-alignment.patch

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Low-level bindings to the zeromq library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       pkgconfig(libzmq) >= 4.3.4

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires
echo 'pkgconfig(libzmq) >= 4.3.4'

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
