# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/xhit/go-str2duration
%global goipath         github.com/xhit/go-str2duration/v2
Version:                2.1.0

%gometa -L -f

%global common_description %{expand:
Convert string to duration in golang.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-xhit-str2duration2
Release:        %autorelease
Summary:        Convert string to duration in golang

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
