# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate ansi_term

Name:           rust-ansi_term0.11
Version:        0.11.0
Release:        %autorelease
Summary:        Library for ANSI terminal colours and styles (bold, underline)

License:        MIT
URL:            https://crates.io/crates/ansi_term
Source:         %{crates_source}
# Automatically generated patch to strip foreign dependencies
Patch:          ansi_term-fix-metadata-auto.diff
# * backport patch to fix a failing test:
#   https://github.com/ogham/rust-ansi-term/commit/5ff6af6
Patch:          0001-Fix-failing-test.patch

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Library for ANSI terminal colours and styles (bold, underline).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENCE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -N
grep -IUlr $'\r' | xargs sed -i 's/\r$//'
%autopatch -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
