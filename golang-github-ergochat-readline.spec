# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/ergochat/readline
%global goipath         github.com/ergochat/readline
Version:                0.1.3

%gometa -L -f

%global common_description %{expand:
A readline implementation in pure Go.}

%global golicenses      LICENSE
%global godocs          docs example README.md

Name:           golang-github-ergochat-readline
Release:        %autorelease
Summary:        A readline implementation in pure Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
