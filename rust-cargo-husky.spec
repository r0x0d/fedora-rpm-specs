# Generated by rust2rpm 26
# * files which are needed to run tests are not included in published crates:
#   https://github.com/rhysd/cargo-husky/issues/17
%bcond_with check
%global debug_package %{nil}

%global crate cargo-husky

Name:           rust-cargo-husky
Version:        1.5.0
Release:        %autorelease
Summary:        Husky for cargo

License:        MIT
URL:            https://crates.io/crates/cargo-husky
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * set "license" instead of "license-file" in crate metadata; "license-file" is
#   intended to be used for non-standard licenses only:
#   https://github.com/rhysd/cargo-husky/pull/15
# * remove reference to readme file that is not included in published crates
Patch:          cargo-husky-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Husky for cargo.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+postmerge-hook-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+postmerge-hook-devel %{_description}

This package contains library source intended for building other packages which
use the "postmerge-hook" feature of the "%{crate}" crate.

%files       -n %{name}+postmerge-hook-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+precommit-hook-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+precommit-hook-devel %{_description}

This package contains library source intended for building other packages which
use the "precommit-hook" feature of the "%{crate}" crate.

%files       -n %{name}+precommit-hook-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+prepush-hook-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+prepush-hook-devel %{_description}

This package contains library source intended for building other packages which
use the "prepush-hook" feature of the "%{crate}" crate.

%files       -n %{name}+prepush-hook-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+run-cargo-check-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+run-cargo-check-devel %{_description}

This package contains library source intended for building other packages which
use the "run-cargo-check" feature of the "%{crate}" crate.

%files       -n %{name}+run-cargo-check-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+run-cargo-clippy-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+run-cargo-clippy-devel %{_description}

This package contains library source intended for building other packages which
use the "run-cargo-clippy" feature of the "%{crate}" crate.

%files       -n %{name}+run-cargo-clippy-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+run-cargo-fmt-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+run-cargo-fmt-devel %{_description}

This package contains library source intended for building other packages which
use the "run-cargo-fmt" feature of the "%{crate}" crate.

%files       -n %{name}+run-cargo-fmt-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+run-cargo-test-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+run-cargo-test-devel %{_description}

This package contains library source intended for building other packages which
use the "run-cargo-test" feature of the "%{crate}" crate.

%files       -n %{name}+run-cargo-test-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+run-for-all-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+run-for-all-devel %{_description}

This package contains library source intended for building other packages which
use the "run-for-all" feature of the "%{crate}" crate.

%files       -n %{name}+run-for-all-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+user-hooks-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+user-hooks-devel %{_description}

This package contains library source intended for building other packages which
use the "user-hooks" feature of the "%{crate}" crate.

%files       -n %{name}+user-hooks-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
