# Generated by rust2rpm 25
%bcond_without check
%global debug_package %{nil}

%global crate rust-stemmers

Name:           rust-rust-stemmers
Version:        1.2.0
Release:        %autorelease
Summary:        Rust implementation of some popular snowball stemming algorithms

# * rust-stemmers: MIT OR BSD-3-Clause
# * test_data: GPL-3.0-or-later
License:        (MIT OR BSD-3-Clause) AND GPL-3.0-or-later
URL:            https://crates.io/crates/rust-stemmers
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A rust implementation of some popular snowball stemming algorithms.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/algorithms/LICENSE
%license %{crate_instdir}/test_data/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
