# Generated by rust2rpm 23
%bcond_without check
%global debug_package %{nil}

%global crate mutate_once

Name:           rust-mutate_once
Version:        0.1.1
Release:        %autorelease
Summary:        Interior mutability, write-once and borrowable as plain &T

License:        BSD-2-Clause
URL:            https://crates.io/crates/mutate_once
Source:         %{crates_source}
# Add missing LICENSE file from upstream
Source1:        https://raw.githubusercontent.com/kamadak/mutate_once-rs/master/LICENSE

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Interior mutability, write-once and borrowable as plain &T.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp %{SOURCE1} LICENSE
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
