name: "Deploy GitHub Pages"

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    name: "Build and deploy site"
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate file list
        run: |
          ls specs/ | sort | jq -R -s 'split("\n") | map(select(. != ""))' > filelist.json

      - name: Assemble site
        run: |
          mkdir -p _site/specs
          cp index.html _site/
          cp filelist.json _site/
          cp specs/* _site/specs/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
