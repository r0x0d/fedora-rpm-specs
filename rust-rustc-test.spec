# Generated by rust2rpm 24
# * tests fail on recent stable versions of Rust:
#   https://github.com/servo/rustc-test/issues/7
%bcond_with check
%global debug_package %{nil}

%global crate rustc-test

Name:           rust-rustc-test
Version:        0.3.1
Release:        %autorelease
Summary:        Fork of Rust’s test crate that doesn’t require unstable language features

# https://github.com/servo/rustc-test/issues/5
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/rustc-test
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump term dependency from 0.4 to 0.6:
#   https://github.com/servo/rustc-test/pull/9
# * bump rustc_version from 0.2 to 0.3:
#   https://github.com/servo/rustc-test/pull/13
Patch:          rustc-test-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A fork of Rust’s `test` crate that doesn’t require unstable language
features.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
# FIXME: no license files detected
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+asm_black_box-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+asm_black_box-devel %{_description}

This package contains library source intended for building other packages which
use the "asm_black_box" feature of the "%{crate}" crate.

%files       -n %{name}+asm_black_box-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+capture-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+capture-devel %{_description}

This package contains library source intended for building other packages which
use the "capture" feature of the "%{crate}" crate.

%files       -n %{name}+capture-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
