# Generated by rust2rpm 22
%bcond_without check
%global debug_package %{nil}

%global crate pathsearch
%global forgeurl https://github.com/wez/wzsh
%global commit 2c5c3bd4676ade9dec2eb3a94f9f2d429cc23b54

Name:           rust-pathsearch
Version:        0.2.0
Release:        %autorelease
Summary:        Search for files in PATH

License:        MIT
URL:            https://crates.io/crates/pathsearch
Source:         %{crates_source}
# Add missing license files: https://github.com/wez/wzsh/pull/6
Source:         %{forgeurl}/raw/%{commit}/LICENSE.md

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Search for files in PATH.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp -p %SOURCE1 .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
