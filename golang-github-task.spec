# Generated by go2rpm 1.6.0
%bcond_without check

# https://github.com/go-task/task
%global goipath         github.com/go-task/task
Version:                3.39.2

%gometa

%global common_description %{expand:
A task runner / simpler Make alternative written in Go.}

%global golicenses      LICENSE
%global godocs          website CHANGELOG.md README.md

Name:           %{goname}
Release:        %autorelease
Summary:        A task runner / simpler Make alternative written in Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description
%{common_description}

%gopkg
%package -n go-task 
Summary: Provides the go-task task-runner binary 
%description -n go-task %{common_description}


%package doc
Summary: Extended documentation for %{name}
%description doc
%{common_description}
 .
 This package contains extended documentation for go-task.


%prep
%goprep

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                        %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/task %{buildroot}%{_bindir}/go-task

%if %{with check}
%check
%gocheck
%endif

%files doc
%license LICENSE
%doc website

%files -n go-task
%license LICENSE
%doc CHANGELOG.md README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
