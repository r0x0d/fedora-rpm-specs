# Generated by rust2rpm 24
# Disable cargo check due to failure in s390x:
# https://github.com/rust-netlink/netlink-packet-generic/issues/14
%bcond_with check
%global debug_package %{nil}

%global crate netlink-packet-generic

Name:           rust-netlink-packet-generic
Version:        0.3.3
Release:        %autorelease
Summary:        Generic netlink packet types

License:        MIT
URL:            https://crates.io/crates/netlink-packet-generic
Source:         %{crates_source}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Generic netlink packet types.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
