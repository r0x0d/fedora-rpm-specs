# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/henvic/httpretty
%global goipath         github.com/henvic/httpretty
Version:                0.1.4

%gometa -L -f

%global common_description %{expand:
Package httpretty prints the HTTP requests you make with Go pretty on your
terminal.}

%global golicenses      LICENSE.md
%global godocs          example CONTRIBUTING.md README.md

Name:           golang-github-henvic-httpretty
Release:        %autorelease
Summary:        Package httpretty prints the HTTP requests you make with Go pretty on your terminal

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
