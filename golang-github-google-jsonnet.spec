# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/google/go-jsonnet
%global goipath         github.com/google/go-jsonnet
Version:                0.20.0

%gometa -L -f

%global common_description %{expand:
Pure Go implementation of the jsonnet JSON extension}

%global golicenses      LICENSE
%global godocs          README.md linter/README.md

Name:           golang-github-google-jsonnet
Release:        %autorelease
Summary:        Golang implementation of jsonnet JSON extension

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%ifnarch s390x %{ix86}
# https://github.com/google/go-jsonnet/issues/767
%check
%gocheck
%endif
%endif
%endif

%if %{without bootstrap}
%files
%license LICENSE
%doc README.md linter/README.md
%endif

%gopkgfiles

%changelog
%autochangelog
