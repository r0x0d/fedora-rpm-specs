# Generated by go2rpm 1.8.0
%bcond_without check

# https://github.com/youpy/go-wav
%global goipath         github.com/youpy/go-wav
Version:                0.3.2

%gometa -f

%global common_description %{expand:
A Go library to read/write WAVE(RIFF waveform Audio) Format.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        A Go library to read/write WAVE(RIFF waveform Audio) Format

License:        ISC
URL:            %{gourl}
Source:         %{gosource}

%if %{with check}
BuildRequires: golang(gotest.tools/v3/assert)
%endif

%description %{common_description}

%gopkg

%prep
%goprep
sed -i 's:gotest.tools/assert:gotest.tools/v3/assert:' reader_test.go

%generate_buildrequires
%go_generate_buildrequires

%build
for cmd in benchmark; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
%gocheck
%endif

%files
%license LICENSE
%doc README.md
%{_bindir}/*

%gopkgfiles

%changelog
%autochangelog
