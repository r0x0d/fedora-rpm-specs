# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%if %{with bootstrap}
%global debug_package %{nil}
%endif

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/microcosm-cc/bluemonday
%global goipath         github.com/microcosm-cc/bluemonday
Version:                1.0.27

%gometa -L

%global common_description %{expand:
Bluemonday is a fast golang HTML sanitizer (inspired by the OWASP Java HTML
Sanitizer) to scrub user generated content of XSS.}

%global golicenses      LICENSE.md
%global godocs          CONTRIBUTING.md CREDITS.md README.md SECURITY.md

Name:           golang-github-microcosm-cc-bluemonday
Release:        %autorelease
Summary:        Fast golang HTML sanitizer

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%if %{without bootstrap}
%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done
%endif

%install
%gopkginstall
%if %{without bootstrap}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/
%endif

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%if %{without bootstrap}
%files
%license LICENSE.md
%doc CONTRIBUTING.md CREDITS.md README.md SECURITY.md
%{_bindir}/*
%endif

%gopkgfiles

%changelog
%autochangelog
