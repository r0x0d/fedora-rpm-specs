# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/peterbourgon/unixtransport
%global goipath         github.com/peterbourgon/unixtransport
Version:                0.0.4

%gometa -L -f

%global common_description %{expand:
Support for Unix domain sockets in Go HTTP clients.}

%global golicenses      LICENSE
%global godocs          README.md unixproxy-README.md

Name:           golang-github-peterbourgon-unixtransport
Release:        %autorelease
Summary:        Support for Unix domain sockets in Go HTTP clients

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

mv unixproxy/README.md unixproxy-README.md

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
