# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%if %{with bootstrap}
%global debug_package %{nil}
%endif

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/liquidweb/liquidweb-cli
%global goipath         github.com/liquidweb/liquidweb-cli
Version:                0.7.0

%gometa -L -f

%global common_description %{expand:
Official command line interface for the LiquidWeb API.}

%global golicenses      COPYRIGHT LICENSE
%global godocs          examples README.md

Name:           lw-cli
Release:        %autorelease
Summary:        Official command line interface for the LiquidWeb API

License:        Apache-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

sed -i 's|github.com/google/go-github/v32|github.com/google/go-github|' $(find . -iname '*.go' -type f)

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%if %{without bootstrap}
%build
%gobuild -o %{gobuilddir}/bin/liquidweb-cli %{goipath}
%endif

%install
%gopkginstall
%if %{without bootstrap}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/
%endif

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%if %{without bootstrap}
%files
%license COPYRIGHT LICENSE
%doc examples README.md
%{_bindir}/liquidweb-cli
%endif

%gopkgfiles

%changelog
%autochangelog
