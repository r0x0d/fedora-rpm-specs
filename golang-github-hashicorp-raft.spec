# Generated by go2rpm 1.14.0
# fails randomly
%ifarch x86_64
%bcond check 1
%else
%bcond check 0
%endif
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/hashicorp/raft
%global goipath         github.com/hashicorp/raft
Version:                1.2.0

%gometa -L

%global common_description %{expand:
Golang implementation of the Raft consensus protocol.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md membership.md

Name:           golang-github-hashicorp-raft
Release:        %autorelease
Summary:        Golang implementation of the Raft consensus protocol

License:        MPL-2.0
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

# Remove fuzzy testing module
rm -rf fuzzy

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
# .: needs network
%gocheck -d .
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
