# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate filedescriptor

Name:           rust-filedescriptor
Version:        0.8.2
Release:        %autorelease
Summary:        More ergonomic wrappers around RawFd and RawHandle

License:        MIT
URL:            https://crates.io/crates/filedescriptor
Source0:        %{crates_source}
# https://github.com/wez/wezterm/pull/2418
Source1:        https://github.com/wez/wezterm/raw/0048209/filedescriptor/LICENSE.md
# Automatically generated patch to strip foreign dependencies
Patch:          filedescriptor-fix-metadata-auto.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
More ergonomic wrappers around RawFd and RawHandle.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
