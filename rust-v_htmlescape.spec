# Generated by rust2rpm 25
%ifarch x86_64 %{ix86}
# Tests use x86-specific intrinsics
%bcond_without check
%else
%bcond_with check
%endif
%global debug_package %{nil}

%global crate v_htmlescape

Name:           rust-v_htmlescape
Version:        0.15.8
Release:        %autorelease
Summary:        Simd optimized HTML escaping code

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/v_htmlescape
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
The simd optimized HTML escaping code.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+buf-min-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+buf-min-devel %{_description}

This package contains library source intended for building other packages which
use the "buf-min" feature of the "%{crate}" crate.

%files       -n %{name}+buf-min-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+bytes-buf-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+bytes-buf-devel %{_description}

This package contains library source intended for building other packages which
use the "bytes-buf" feature of the "%{crate}" crate.

%files       -n %{name}+bytes-buf-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
