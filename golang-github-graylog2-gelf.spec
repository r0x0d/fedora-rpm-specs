# Generated by go2rpm 1.8.2
%bcond_without check
%global debug_package %{nil}

# https://github.com/Graylog2/go-gelf
%global goipath         github.com/Graylog2/go-gelf
Version:                2.0.0
%global commit          1550ee647df0510058c9d67a45c56f18911d80b8

%gometa

%global goaltipaths     gopkg.in/Graylog2/go-gelf.v2

%global common_description %{expand:
GELF (Graylog Extended Log Format) is an application-level logging protocol that
avoids many of the shortcomings of syslog. While it can be run over any stream
or datagram transport protocol, it has special support (chunking) to allow long
messages to be split over multiple datagrams.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        GELF library for Go

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
