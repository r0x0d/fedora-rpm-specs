# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate compose_spec_macros

Name:           rust-compose_spec_macros
Version:        0.1.0
Release:        %autorelease
Summary:        Procedural macros for use in the compose_spec crate

License:        MPL-2.0
URL:            https://crates.io/crates/compose_spec_macros
Source:         %{crates_source}
# * license file corresponding to the commit from which v0.1.0 was published:
#   https://github.com/k9withabone/compose_spec_rs/issues/21
Source:         https://github.com/k9withabone/compose_spec_rs/raw/bb62265/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Procedural macros for use in the compose_spec crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
