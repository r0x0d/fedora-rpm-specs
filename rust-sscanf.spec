# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate sscanf

Name:           rust-sscanf
Version:        0.4.2
Release:        %autorelease
Summary:        Sscanf (inverse of format!()) Macro based on Regex

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/sscanf
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          sscanf-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A sscanf (inverse of format!()) Macro based on Regex.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/Changelog.md
%doc %{crate_instdir}/Readme.md
%doc %{crate_instdir}/todo.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip failing tests and a single hex number test that was failing on i686
#   (Issue filed: https://github.com/mich101mich/sscanf/issues/10)
%cargo_test -- -- --skip failing_tests --skip types::hex_number::hex_number --skip types::HexNumber
%endif

%changelog
%autochangelog
