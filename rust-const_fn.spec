# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate const_fn

Name:           rust-const_fn
Version:        0.4.10
Release:        %autorelease
Summary:        Attribute for easy generation of const functions with conditional compilations

License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/const_fn
Source:         %{crates_source}
# * drop deny(warnings) from doctests to fix issues with Rust 1.80+
Patch:          0001-drop-deny-warnings-from-doctests-to-fix-issues-with-.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A lightweight attribute for easy generation of const functions with
conditional compilations.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
