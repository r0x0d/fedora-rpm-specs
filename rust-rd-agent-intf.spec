# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate rd-agent-intf

Name:           rust-rd-agent-intf
Version:        2.2.5
Release:        %autorelease
Summary:        Management agent for resctl-demo (interface library)

License:        Apache-2.0
URL:            https://crates.io/crates/rd-agent-intf
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump enum-iterator to 2.0
Patch:          rd-agent-intf-fix-metadata.diff
# https://github.com/facebookexperimental/resctl-demo/commit/f65eedf9017332255ef0a48649664c26b45b9c07
Patch:          rd-agent-intf-enum-iterator.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Management agent for resctl-demo (interface library).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
