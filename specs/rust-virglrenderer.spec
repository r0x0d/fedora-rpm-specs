# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate virglrenderer

Name:           rust-virglrenderer
Version:        0.1.3
Release:        %autorelease
Summary:        Safe and idiomatic Rust wrapper for virglrenderer

License:        MIT
URL:            https://crates.io/crates/virglrenderer
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
Patch:          virglrenderer-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  pkgconfig(virglrenderer) >= 0.10

%global _description %{expand:
Safe and idiomatic Rust wrapper for virglrenderer.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       pkgconfig(virglrenderer) >= 0.10

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+virgl_renderer_unstable-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+virgl_renderer_unstable-devel %{_description}

This package contains library source intended for building other packages which
use the "virgl_renderer_unstable" feature of the "%{crate}" crate.

%files       -n %{name}+virgl_renderer_unstable-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
