# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate picky-test-data

Name:           rust-picky-test-data
Version:        0.1.0
Release:        %autorelease
Summary:        Test data for the picky crates

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/picky-test-data
Source:         %{crates_source}
# https://github.com/Devolutions/picky-rs/issues/332
# the "test_assets" files are either generated manually, or a certificate chain from some other provider to test some edge case
Source1:        https://github.com/Devolutions/picky-rs/raw/picky-test-data-0.1.0/LICENSE-APACHE
Source2:        https://github.com/Devolutions/picky-rs/raw/picky-test-data-0.1.0/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Test data for the picky crates.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
