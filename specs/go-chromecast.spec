# Generated by go2rpm 1.19.0
%bcond check 1

# https://github.com/vishen/go-chromecast
%global goipath         github.com/vishen/go-chromecast
%global release_commit  408c42e94c13548c0a65748b150891b35b626f72
Version:                0.3.4

%gometa -L -f


Name:           go-chromecast
Release:        %autorelease
Summary:        CLI for Google Chromecast, Home devices and Cast Groups

# Generated by go-vendor-tools
License:        Apache-2.0 AND BSD-2-Clause AND BSD-3-Clause AND ISC AND MIT
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

BuildRequires:  go-vendor-tools

# Optionally used for transcoding
Recommends:     ffmpeg

%description
CLI for Google Chromecast, Home devices and Cast Groups.

%prep
%goprep -p1
tar -xf %{S:1}

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
%global gomodulesmode GO111MODULE=on
export GO_LDFLAGS="-X main.version=%{version} \
                   -X main.commit=%{release_commit} \
                   -X main.date=$(date -d "@${SOURCE_DATE_EPOCH}" +%Y-%m-%dT%H:%M:%SZ)"
%gobuild -o %{gobuilddir}/bin/go-chromecast %{goipath}

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
%gotest ./...
%endif

%files -f %{go_vendor_license_filelist}
%doc README.md
%{_bindir}/go-chromecast


%changelog
%autochangelog
