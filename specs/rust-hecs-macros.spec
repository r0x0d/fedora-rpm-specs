# Generated by rust2rpm 28
# * tests can only be compiled and run in-workspace
%bcond check 0
%global debug_package %{nil}

%global crate hecs-macros

Name:           rust-hecs-macros
Version:        0.10.0
Release:        %autorelease
Summary:        Procedural macro definitions for hecs

License:        Apache-2.0
URL:            https://crates.io/crates/hecs-macros
Source:         %{crates_source}
# * https://github.com/Ralith/hecs/pull/433
Source2:        https://github.com/Ralith/hecs/raw/refs/tags/v0.10.5/LICENSE-APACHE
# * https://github.com/Ralith/hecs/pull/433
Source3:        https://github.com/Ralith/hecs/raw/refs/tags/v0.10.5/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Procedural macro definitions for hecs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE2} %{SOURCE3} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
