# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate cryptographic-message-syntax

Name:           rust-cryptographic-message-syntax
Version:        0.26.0
Release:        %autorelease
Summary:        Pure Rust implementation of Crypographic Message Syntax (RFC 5652)

License:        MPL-2.0
URL:            https://crates.io/crates/cryptographic-message-syntax
Source:         %{crates_source}
# * https://github.com/indygreg/cryptography-rs/pull/23
Source2:        https://github.com/indygreg/cryptography-rs/raw/cfaafe05892ce5c617dcac593a9224613bf88a0e/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A pure Rust implementation of Crypographic Message Syntax (RFC 5652).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp -p %SOURCE2 .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * Skip tests requiring Internet access
%cargo_test -- -- --skip time_stamp_protocol::test::simple_request --skip signing::tests::time_stamp_url
%endif

%changelog
%autochangelog
