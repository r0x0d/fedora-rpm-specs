# Generated by rust2rpm 26
# * tests can only be run in-tree
%bcond_with check
%global debug_package %{nil}

%global crate gtk4-macros

Name:           rust-gtk4-macros0.8
Version:        0.8.2
Release:        %autorelease
Summary:        Macros helpers for GTK 4 bindings

License:        MIT
URL:            https://crates.io/crates/gtk4-macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Macros helpers for GTK 4 bindings.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/COPYRIGHT
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+blueprint-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+blueprint-devel %{_description}

This package contains library source intended for building other packages which
use the "blueprint" feature of the "%{crate}" crate.

%files       -n %{name}+blueprint-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+quick-xml-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+quick-xml-devel %{_description}

This package contains library source intended for building other packages which
use the "quick-xml" feature of the "%{crate}" crate.

%files       -n %{name}+quick-xml-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+xml_validation-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+xml_validation-devel %{_description}

This package contains library source intended for building other packages which
use the "xml_validation" feature of the "%{crate}" crate.

%files       -n %{name}+xml_validation-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
