# Generated by go2rpm 1.19.0
%bcond check 1

# https://github.com/hickford/git-credential-oauth
%global goipath         github.com/hickford/git-credential-oauth
Version:                0.17.2

%gometa -L -f


Name:           git-credential-oauth
Release:        %autorelease
Summary:        A Git credential helper that securely authenticates to GitHub, GitLab and BitBucket using OAuth

# Generated by go-vendor-tools
License:        Apache-2.0 AND BSD-3-Clause
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

Enhances: git
Recommends: xdg-utils

BuildRequires:  go-vendor-tools

%description
A Git credential helper that securely authenticates to GitHub, GitLab and
BitBucket using OAuth. The first time you push, the helper will open a
browser window to authenticate. Subsequent pushes require no interaction.

%prep
%goprep -p1
tar -xf %{S:1}

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
export GO_LDFLAGS="-X main.version=%{version}"
%global gomodulesmode GO111MODULE=on
%gobuild -o %{gobuilddir}/bin/git-credential-oauth %{goipath}

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/
install -m 0755 -vd                     %{buildroot}%{_mandir}/man1
install -m 0644 -vp git-credential-oauth.1 -t %{buildroot}%{_mandir}/man1

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
%gotest ./...
%endif

%files -f %{go_vendor_license_filelist}
%{_mandir}/man1/git-credential-oauth.1*
%doc CONTRIBUTING.md README.md
%{_bindir}/git-credential-oauth


%changelog
%autochangelog
