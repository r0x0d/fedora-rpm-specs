# Generated by rust2rpm 26
%bcond_without check

%global crate libbpf-cargo

Name:           rust-libbpf-cargo
Version:        0.24.4
Release:        %autorelease
Summary:        Cargo plugin to build bpf programs

License:        LGPL-2.1-only OR BSD-2-Clause
URL:            https://crates.io/crates/libbpf-cargo
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Update goblin dependency to 0.9:
#   https://github.com/libbpf/libbpf-rs/pull/988
Patch:          libbpf-cargo-fix-metadata.diff
# * by default, test.rs expects libbpf-rs to be in the same checkout
# * fix to point to /usr/share/cargo/registry
Patch2:        libbpf-cargo-fix-finding-libbpf-rs.diff
# * https://github.com/libbpf/libbpf-rs/issues/790
# * Revert the commit that refactors vmlinux.h to unshipped crate
Patch3:        libbpf-cargo-revert-vmlinux-refactor.diff

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  clang
BuildRequires:  rustfmt

%global _description %{expand:
Cargo plugin to build bpf programs.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0 OR BSL-1.0
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# BSD-2-Clause
# LGPL-2.1-only OR BSD-2-Clause
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
License:        (LGPL-2.1-only OR BSD-2-Clause) AND (MIT OR Apache-2.0) AND Unicode-DFS-2016 AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND BSD-2-Clause AND MIT AND (Unlicense OR MIT)
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.BSD-2-Clause
%license LICENSE.LGPL-2.1
%license LICENSE.dependencies
%doc CHANGELOG.md
%doc README.md
%{_bindir}/cargo-libbpf

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       clang
Requires:       rustfmt

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/LICENSE.BSD-2-Clause
%license %{crate_instdir}/LICENSE.LGPL-2.1
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
# * these do not work with the reverted vmlinux.h
%cargo_test -- -- --exact --skip test::test_btf_dump_definition_struct_ops_mixed --skip test::test_skeleton_generate_struct_with_pointer_array --skip test::test_skeleton_multipl_kfuncs
%endif

%changelog
%autochangelog
