# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate lapin

Name:           rust-lapin
Version:        4.10.0
Release:        %autorelease
Summary:        AMQP client library

License:        MIT
URL:            https://crates.io/crates/lapin
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Flip default feature from rustls to openssl
# * Relax flume dependency as it doesn't used the feature that changed names
Patch:          lapin-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
AMQP client library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/AGENTS.md
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+async-global-executor-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+async-global-executor-devel %{_description}

This package contains library source intended for building other packages which
use the "async-global-executor" feature of the "%{crate}" crate.

%files       -n %{name}+async-global-executor-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+default-runtime-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-runtime-devel %{_description}

This package contains library source intended for building other packages which
use the "default-runtime" feature of the "%{crate}" crate.

%files       -n %{name}+default-runtime-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+hickory-dns-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+hickory-dns-devel %{_description}

This package contains library source intended for building other packages which
use the "hickory-dns" feature of the "%{crate}" crate.

%files       -n %{name}+hickory-dns-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+openssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+openssl-devel %{_description}

This package contains library source intended for building other packages which
use the "openssl" feature of the "%{crate}" crate.

%files       -n %{name}+openssl-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+smol-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+smol-devel %{_description}

This package contains library source intended for building other packages which
use the "smol" feature of the "%{crate}" crate.

%files       -n %{name}+smol-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio" feature of the "%{crate}" crate.

%files       -n %{name}+tokio-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * Integration tests require RabbitMQ to be running
%cargo_test -- --lib
%cargo_test -- --doc
%endif

%changelog
%autochangelog
