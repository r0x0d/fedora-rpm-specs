# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate pcg-mwc

Name:           rust-pcg-mwc
Version:        0.2.1
Release:        %autorelease
Summary:        Fast non-cryptographic psudo random number generator

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/pcg-mwc
Source:         %{crates_source}
# https://github.com/tkaitchuck/Mwc256XXA64/pull/6
Source1:        https://raw.githubusercontent.com/tkaitchuck/Mwc256XXA64/f668e18bdb055c6ae2c3ca74ff587732ff64532c/LICENSE-APACHE
Source2:        https://raw.githubusercontent.com/tkaitchuck/Mwc256XXA64/f668e18bdb055c6ae2c3ca74ff587732ff64532c/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A fast non-cryptographic psudo random number generator.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde1-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde1-devel %{_description}

This package contains library source intended for building other packages which
use the "serde1" feature of the "%{crate}" crate.

%files       -n %{name}+serde1-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp -pav %{SOURCE1} %{SOURCE2} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
