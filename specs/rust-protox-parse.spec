# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate protox-parse

Name:           rust-protox-parse
Version:        0.9.0
Release:        %autorelease
Summary:        Parsing of protobuf source files

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/protox-parse
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Relax logos dependency
Patch:          protox-parse-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Parsing of protobuf source files.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# Tests fail misserably without it
# TODO: Check with upstream if there are better approaches otherwise
# https://github.com/andrewhickman/protox/issues/102
export INSTA_UPDATE=always
%cargo_test
%endif

%changelog
%autochangelog
