# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/AdamSLevy/jsonrpc2
%global goipath         github.com/AdamSLevy/jsonrpc2/v14
Version:                14.1.0

%gometa -L -f

%global common_description %{expand:
Golang package for implementing a JSON RPC 2.0 server or client.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-adamslevy-jsonrpc2_14
Release:        %autorelease
Summary:        Golang package for implementing a JSON RPC 2.0 server or client

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
