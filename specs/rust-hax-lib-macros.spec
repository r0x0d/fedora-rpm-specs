# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate hax-lib-macros

Name:           rust-hax-lib-macros
Version:        0.3.7
Release:        %autorelease
Summary:        Hax-specific proc-macros for Rust programs

License:        Apache-2.0
URL:            https://crates.io/crates/hax-lib-macros
Source:         %{crates_source}
# * Fix missing LICENSE file in published crate.
# https://github.com/cryspen/hax/pull/2106
Source10:       https://github.com/cryspen/hax/raw/refs/tags/hax-lib-macros-v%{version}/LICENSE
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          hax-lib-macros-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Hax-specific proc-macros for Rust programs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp -p %{S:10} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
