# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

# prevent executables from being installed
%global cargo_install_bin 0

%global crate rustbus

Name:           rust-rustbus
Version:        0.17.0
Release:        %autorelease
Summary:        Implementation of the dbus protocol

License:        MIT
URL:            https://crates.io/crates/rustbus
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump nix dependency from 0.22 to 0.28
# * drop unused, benchmark-only criterion dev-dependency
Patch:          rustbus-fix-metadata.diff
# * port to nix 0.24: https://github.com/KillingSpark/rustbus/commit/8611c32
Patch2:         0001-port-to-nix-0.24.patch
# * port to nix 0.26: https://github.com/KillingSpark/rustbus/commit/3c1df10
Patch3:         0002-port-to-nix-0.26.patch
# * port to nix 0.27: https://github.com/KillingSpark/rustbus/commit/463308f
Patch4:         0003-port-to-nix-0.27.patch
# * port to nix 0.28: https://github.com/KillingSpark/rustbus/commit/a51794a
Patch5:         0004-port-to-nix-0.28.patch

BuildRequires:  cargo-rpm-macros >= 26

%global _description %{expand:
An implementation of the dbus protocol.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip a test that requires a running dbus daemon
%cargo_test -- -- --exact --skip tests::fdpassing::test_fd_passing
%endif

%changelog
%autochangelog
