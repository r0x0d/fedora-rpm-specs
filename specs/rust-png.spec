# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate png

Name:           rust-png
Version:        0.18.1
Release:        %autorelease
Summary:        PNG decoding and encoding library in pure Rust

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/png
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop unstable and benchmarking-only features
# * drop unused, benchmark-only criterion dev-dependency
Patch:          png-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
PNG decoding and encoding library in pure Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGES.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zlib-rs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zlib-rs-devel %{_description}

This package contains library source intended for building other packages which
use the "zlib-rs" feature of the "%{crate}" crate.

%files       -n %{name}+zlib-rs-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * data files for some tests are not included in published crates
%{cargo_test -- --lib -- --exact %{shrink:
    --skip decoder::stream::tests::image_gamma
    --skip decoder::stream::tests::image_source_chromaticities
    --skip decoder::stream::tests::image_source_sbit
    --skip decoder::stream::tests::test_cicp_mdcv_and_clli_chunks
    --skip decoder::stream::tests::test_exif_chunk
    --skip decoder::stream::tests::test_png_with_broken_iccp
    --skip decoder::stream::tests::test_two_iccp_chunks
    --skip encoder::tests::image_palette
}}
%endif

%changelog
%autochangelog
