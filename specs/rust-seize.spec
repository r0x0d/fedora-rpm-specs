# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate seize

Name:           rust-seize
Version:        0.5.1
Release:        %autorelease
Summary:        Fast, efficient, predictable memory reclamation for concurrent data structures

License:        MIT AND (MIT OR Apache-2.0)
URL:            https://crates.io/crates/seize
Source:         %{crates_source}
# * Apache-2.0 license text, for the two files in src/raw/tls licensed “MIT OR
#   Apache-2.0”; see discussion above the cargo metadata patch.
Source10:       https://www.apache.org/licenses/LICENSE-2.0.txt
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          seize-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * Drop unused, benchmark-only dev-dependencies on criterion, crossbeam-epoch,
#   and haphazard
# * Adjust the license expression from “MIT” to “MIT AND (MIT OR Apache-2.0)” to
#   reflect the two files in src/raw/tls, apparently derived from an unknown
#   version of the thread_local crate, and Fedora’s policy of enumerating all
#   licenses that contribute to binary RPMs. We do this downstream-only because
#   it seems clear that upstream intended to use these under the “MIT” option to
#   be consistent with the overall MIT license of the crate, and we think it’s
#   unlikely that a PR to pedantically advertise the other license option for
#   these two source files would be of interest. See discussion in
#   https://bugzilla.redhat.com/show_bug.cgi?id=2490673.
Patch:          seize-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Fast, efficient, and predictable memory reclamation for concurrent data
structures.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/LICENSE-2.0.txt
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fast-barrier-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fast-barrier-devel %{_description}

This package contains library source intended for building other packages which
use the "fast-barrier" feature of the "%{crate}" crate.

%files       -n %{name}+fast-barrier-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+libc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+libc-devel %{_description}

This package contains library source intended for building other packages which
use the "libc" feature of the "%{crate}" crate.

%files       -n %{name}+libc-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp --preserve '%{SOURCE10}' .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
