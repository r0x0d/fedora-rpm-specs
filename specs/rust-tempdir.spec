# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate tempdir

Name:           rust-tempdir
Version:        0.3.7
Release:        %autorelease
Summary:        Library for managing a temporary directory

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/tempdir
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump rand from 0.4 to 0.8 (not upstreamable, project dead)
# * bump remove_dir_all from 0.5 to 0.7 (not upstreamable, project dead)
Patch:          tempdir-fix-metadata.diff
Patch:          0001-Port-to-rand-0.8.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A library for managing a temporary directory and deleting all contents
when it's dropped.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
