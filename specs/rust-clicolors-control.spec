# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate clicolors-control

Name:           rust-clicolors-control
Version:        1.0.1
Release:        %autorelease
Summary:        Common utility library to control CLI colorization

# https://github.com/mitsuhiko/clicolors-control/issues/9
License:        MIT
URL:            https://crates.io/crates/clicolors-control
Source0:        %{crates_source}
Source1:        https://github.com/mitsuhiko/clicolors-control/raw/3bbe414/LICENSE
# Automatically generated patch to strip foreign dependencies
Patch:          clicolors-control-fix-metadata-auto.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A common utility library to control CLI colorization.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+terminal_autoconfig-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+terminal_autoconfig-devel %{_description}

This package contains library source intended for building other packages which
use the "terminal_autoconfig" feature of the "%{crate}" crate.

%files       -n %{name}+terminal_autoconfig-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp -pav %{SOURCE1} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
