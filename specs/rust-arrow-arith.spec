# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate arrow-arith

Name:           rust-arrow-arith
Version:        57.3.0
Release:        %autorelease
Summary:        Arrow arithmetic kernels

License:        Apache-2.0
URL:            https://crates.io/crates/arrow-arith
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Arrow arithmetic kernels.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%license %{crate_instdir}/NOTICE.txt
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%ifarch s390x
# * skip some s390x tests for endianness issues
%{cargo_test -- -- %{shrink:
    --skip boolean::and_not
    --skip boolean::is_null
    --skip boolean::not
    --skip aggregate::tests::test_binary_min_max_1
    --skip aggregate::tests::test_binary_min_max_no_null
    --skip aggregate::tests::test_binary_min_max_with_nulls
    --skip aggregate::tests::test_string_min_max_1
    --skip aggregate::tests::test_string_min_max_no_null
    --skip aggregate::tests::test_string_min_max_with_nulls
    --skip boolean::tests::test_bool_array_and_not
    --skip boolean::tests::test_bool_array_not
    --skip boolean::tests::test_bool_array_not_sliced
    --skip boolean::tests::test_dense_union_is_null
    --skip boolean::tests::test_nullable_array_is_null
    --skip boolean::tests::test_nullable_array_with_offset_is_null
    --skip boolean::tests::test_sparse_union_is_null
}}
%else
%cargo_test
%endif
%endif

%changelog
%autochangelog
