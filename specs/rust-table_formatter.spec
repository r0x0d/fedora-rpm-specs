# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate table_formatter

Name:           rust-table_formatter
Version:        0.6.1
Release:        %autorelease
Summary:        Simple text table formatter written in Rust

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/table_formatter
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Update to itertools 0.14, thiserror 2, and colored 3:
#   https://github.com/Embers-of-the-Fire/rust-table-formatter/pull/17
Patch:          table_formatter-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  dos2unix

%global _description %{expand:
A simple text table formatter written in Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
# The git repository has LF line terminations, but the crate has CRLF line
# terminations. The release process was probably done on a Windows machine.
find . -type f | xargs -r -t dos2unix --keepdate
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
