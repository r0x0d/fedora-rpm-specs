# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate keyvalues-serde

Name:           rust-keyvalues-serde
Version:        0.2.1
Release:        %autorelease
Summary:        (De)serialize VDF text with serde

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/keyvalues-serde
Source:         %{crates_source}
# https://github.com/CosmicHorrorDev/vdf-rs/issues/74
Source100:      https://raw.githubusercontent.com/CosmicHorrorDev/vdf-rs/refs/heads/main/LICENSE-MIT
Source101:      https://raw.githubusercontent.com/CosmicHorrorDev/vdf-rs/refs/heads/main/LICENSE-APACHE
# Manually created patch for downstream crate metadata changes
Patch:          keyvalues-serde-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
(De)serialize VDF text with serde.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE            
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
#Copy LICENSE files
cp -pav %{SOURCE100} %{SOURCE101} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
