# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate arrow-row

Name:           rust-arrow-row
Version:        57.3.0
Release:        %autorelease
Summary:        Arrow row format

License:        Apache-2.0
URL:            https://crates.io/crates/arrow-row
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Arrow row format.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%license %{crate_instdir}/NOTICE.txt
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%ifarch s390x
# * skip some s390x tests for endianness issues
%{cargo_test -- -- %{shrink:
    --skip run::tests::test_run_end_encoded_round_trip_strings_with_nulls
    --skip tests::fuzz_test
    --skip tests::test_bool
    --skip tests::test_clear
    --skip tests::test_decimal128
    --skip tests::test_decimal256
    --skip tests::test_decimal32
    --skip tests::test_decimal64
    --skip tests::test_dictionary_in_struct
    --skip tests::test_fixed_size_list
    --skip tests::test_fixed_size_list_with_variable_width_content
    --skip tests::test_fixed_width
    --skip tests::test_large_list
    --skip tests::test_list
    --skip tests::test_list_of_string_dictionary
    --skip tests::test_primitive_dictionary
    --skip tests::test_row_converter_roundtrip_with_many_union_columns
    --skip tests::test_row_converter_roundtrip_with_one_union_column
    --skip tests::test_string_dictionary
    --skip tests::test_struct
    --skip tests::test_two_fixed_size_lists
    --skip tests::test_variable_width
}}
%else
%cargo_test
%endif
%endif

%changelog
%autochangelog
