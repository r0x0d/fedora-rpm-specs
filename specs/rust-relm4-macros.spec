# Generated by rust2rpm 28
# * error[E0432]: unresolved import `relm4` / use of undeclared crate or module
#   `relm4`: tests are unable to run outside of a git checkout due to circular
#   dependencies (relm4-macros's own tests depend on relm4, which depends on
#   relm4-macros).
%bcond check 0
%global debug_package %{nil}

%global crate relm4-macros

Name:           rust-relm4-macros
Version:        0.11.0
Release:        %autorelease
Summary:        Idiomatic GUI library inspired by Elm and based on gtk4-rs

License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/relm4-macros
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An idiomatic GUI library inspired by Elm and based on gtk4-rs.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+relm4-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+relm4-devel %{_description}

This package contains library source intended for building other packages which
use the "relm4" feature of the "%{crate}" crate.

%files       -n %{name}+relm4-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
