# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate linux_proc

Name:           rust-linux_proc
Version:        0.1.1
Release:        %autorelease
Summary:        Library to help reading the contents of /proc on linux

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/linux_proc
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * include license texts in installed files
# * drop quicli dependency as it's only needed for examples
Patch:          linux_proc-fix-metadata.diff
# Backport of https://github.com/derekdreery/linux_proc/pull/2
Patch:          linux_proc-license.patch

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A library to help reading the contents of `/proc` on linux.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
