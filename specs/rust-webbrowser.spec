# Generated by rust2rpm 26
# * Most of the tests require a browser
# * Missing dependencies: actix-files
%bcond_with check
%global debug_package %{nil}

%global crate webbrowser

Name:           rust-webbrowser
Version:        1.0.2
Release:        %autorelease
Summary:        Open URLs in web browsers available on a platform

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/webbrowser
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          webbrowser-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Open URLs in web browsers available on a platform.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/SECURITY.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+disable-wsl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+disable-wsl-devel %{_description}

This package contains library source intended for building other packages which
use the "disable-wsl" feature of the "%{crate}" crate.

%files       -n %{name}+disable-wsl-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+hardened-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+hardened-devel %{_description}

This package contains library source intended for building other packages which
use the "hardened" feature of the "%{crate}" crate.

%files       -n %{name}+hardened-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
