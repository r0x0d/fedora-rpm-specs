# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate evmap

Name:           rust-evmap
Version:        11.0.0
Release:        %autorelease
Summary:        Lock-free, eventually consistent, concurrent multi-value map

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/evmap
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A lock-free, eventually consistent, concurrent multi-value map.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+amortize-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+amortize-devel %{_description}

This package contains library source intended for building other packages which
use the "amortize" feature of the "%{crate}" crate.

%files       -n %{name}+amortize-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+eviction-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+eviction-devel %{_description}

This package contains library source intended for building other packages which
use the "eviction" feature of the "%{crate}" crate.

%files       -n %{name}+eviction-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+indexed-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+indexed-devel %{_description}

This package contains library source intended for building other packages which
use the "indexed" feature of the "%{crate}" crate.

%files       -n %{name}+indexed-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+indexmap-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+indexmap-devel %{_description}

This package contains library source intended for building other packages which
use the "indexmap" feature of the "%{crate}" crate.

%files       -n %{name}+indexmap-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+indexmap-amortized-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+indexmap-amortized-devel %{_description}

This package contains library source intended for building other packages which
use the "indexmap-amortized" feature of the "%{crate}" crate.

%files       -n %{name}+indexmap-amortized-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rand-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rand-devel %{_description}

This package contains library source intended for building other packages which
use the "rand" feature of the "%{crate}" crate.

%files       -n %{name}+rand-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
