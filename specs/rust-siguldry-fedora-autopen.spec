# Generated by rust2rpm 28
%bcond check 1

%global crate siguldry-fedora-autopen

Name:           rust-siguldry-fedora-autopen
Version:        0.2.1
Release:        %autorelease
Summary:        AMQP client to auto-sign Fedora build artifacts

License:        MIT
URL:            https://crates.io/crates/siguldry-fedora-autopen
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  systemd-rpm-macros
%if %{with check}
BuildRequires:  ostree
%endif

%global _description %{expand:
An AMQP client to auto-sign Fedora build artifacts.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
### BEGIN LICENSE SUMMARY ###
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# BSD-2-Clause
# BSD-2-Clause OR Apache-2.0 OR MIT
# BSD-3-Clause
# BSL-1.0
# LGPL-2.0-or-later
# MIT
# MIT AND Apache-2.0
# MIT OR Apache-2.0
# Unicode-3.0
# Unlicense OR MIT
# Zlib
###  END LICENSE SUMMARY  ###
License:        ((MIT OR Apache-2.0) AND Unicode-DFS-2016) AND (Apache-2.0) AND (Apache-2.0 OR MIT) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND (BSD-2-Clause) AND (BSD-2-Clause OR Apache-2.0 OR MIT) AND (BSD-3-Clause) AND (BSL-1.0) AND (LGPL-2.0-or-later) AND (MIT) AND (Unicode-3.0) AND (Unlicense OR MIT) AND (Zlib)
# LICENSE.dependencies contains a full license breakdown
Requires:       siguldry-pkcs11

# Can be used for general OpenPGP signing and RPMs
Recommends:        sequoia-sq

# Needed for signing OSTree
Recommends:        ostree

# Needed for signing RPMs
Recommends:        rpm-sign

# Needed for signing OSTree, can be used for RPMs
Recommends:        gpg
Recommends:        gpg-pkcs11-scd

%post -n %{crate}
%systemd_post siguldry-fedora-autopen.service

%preun -n %{crate}
%systemd_preun siguldry-fedora-autopen.service

%postun -n %{crate}
%systemd_postun_with_restart siguldry-fedora-autopen.service

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc CHANGELOG.md
%doc README.md
%{_bindir}/siguldry-fedora-autopen
%config(noreplace) %{_sysconfdir}/siguldry/fedora-autopen.toml
%{_unitdir}/siguldry-fedora-autopen.service

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
install -D -p -m 0644 config.toml.example %{buildroot}%{_sysconfdir}/siguldry/fedora-autopen.toml
install -D -p -m 0644 siguldry-fedora-autopen.service %{buildroot}%{_unitdir}/siguldry-fedora-autopen.service

%if %{with check}
%check
export RUST_TEST_THREADS=1
# * Integration tests require the network
%cargo_test -- --bins
%endif

%changelog
%autochangelog
