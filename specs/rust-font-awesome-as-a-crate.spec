# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate font-awesome-as-a-crate

Name:           rust-font-awesome-as-a-crate
Version:        0.3.1
Release:        %autorelease
Summary:        Font Awesome Free, packaged as a crate

License:        CC-BY-4.0 AND MIT
URL:            https://crates.io/crates/font-awesome-as-a-crate
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Font Awesome Free, packaged as a crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/fontawesome-free-6.2.0-desktop/LICENSE.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/
%exclude %{crate_instdir}/released.sh

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
