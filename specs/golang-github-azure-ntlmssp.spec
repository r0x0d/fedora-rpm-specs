# Generated by go2rpm 1.14.0
%bcond check 0
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/Azure/go-ntlmssp
%global goipath         github.com/Azure/go-ntlmssp
%global commit          754e69321358ada85ce213a4ec971d3e4d1bfdf7

%gometa -L -f

%global common_description %{expand:
NTLM/Negotiate authentication over HTTP.}

%global golicenses      LICENSE
%global godocs          README.md SECURITY.md

Name:           golang-github-azure-ntlmssp
Version:        0
Release:        %autorelease -p
Summary:        NTLM/Negotiate authentication over HTTP

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
