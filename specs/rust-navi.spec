# Generated by rust2rpm 27
%bcond check 1

%global crate navi

Name:           rust-navi
Version:        2.20.1
Release:        %autorelease
Summary:        Interactive cheatsheet tool for the command-line

License:        Apache-2.0
URL:            https://crates.io/crates/navi
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump crossterm dependency from 0.23 to 0.28
Patch:          navi-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An interactive cheatsheet tool for the command-line.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0
# Apache-2.0 OR BSL-1.0
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# CC0-1.0
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
License:        Apache-2.0 AND CC0-1.0 AND MIT AND Unicode-DFS-2016 AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 OR MIT) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND (Unlicense OR MIT)
# LICENSE.dependencies contains a full license breakdown
Requires:       fzf

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/navi

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+disable-command-execution-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+disable-command-execution-devel %{_description}

This package contains library source intended for building other packages which
use the "disable-command-execution" feature of the "%{crate}" crate.

%files       -n %{name}+disable-command-execution-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+disable-repo-management-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+disable-repo-management-devel %{_description}

This package contains library source intended for building other packages which
use the "disable-repo-management" feature of the "%{crate}" crate.

%files       -n %{name}+disable-repo-management-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
