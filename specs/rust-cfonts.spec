# Generated by rust2rpm 27
%bcond check 1

%global crate cfonts

Name:           rust-cfonts
Version:        1.3.0
Release:        %autorelease
Summary:        Sexy ANSI fonts for the console

License:        GPL-3.0-or-later
URL:            https://crates.io/crates/cfonts
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Omit the enable-ansi-support dependency; see “Make the enable-ansi-support
#   dependency Windows-specific,”
#   https://github.com/dominikwilkowski/cfonts/pull/89
Patch:          cfonts-fix-metadata.diff
# * “Make the enable-ansi-support dependency Windows-specific,”
#   https://github.com/dominikwilkowski/cfonts/pull/89; this is the source-code
#   change to conditionalize uses of the crate.
Patch10:        0001-Make-the-enable-ansi-support-dependency-Windows-spec.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Sexy ANSI fonts for the console.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Apache-2.0
# Apache-2.0 OR BSL-1.0
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# BSD-2-Clause OR Apache-2.0 OR MIT
# GPL-3.0-or-later
# ISC
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
License:        %{shrink:
    GPL-3.0-or-later AND
    Apache-2.0 AND
    ISC AND
    MIT AND
    (Apache-2.0 OR BSL-1.0) AND
    (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND
    (BSD-2-Clause OR Apache-2.0 OR MIT) AND
    (MIT OR Apache-2.0) AND
    (Unlicense OR MIT)
    }

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/cfonts

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
