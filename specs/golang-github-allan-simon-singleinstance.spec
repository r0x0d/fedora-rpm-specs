# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/allan-simon/go-singleinstance
%global goipath         github.com/allan-simon/go-singleinstance
Version:                1.0.0
%global tag             1.0.0

%gometa -L -f

%global common_description %{expand:
Cross plateform library to have only one instance of a software (based on
python's tendo).}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-allan-simon-singleinstance
Release:        %autorelease
Summary:        Cross plateform library to have only one instance of a software

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
