# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate expat-sys

Name:           rust-expat-sys
Version:        2.1.6
Release:        %autorelease
Summary:        XML parser library written in C

# https://github.com/servo/libexpat/issues/28
# https://github.com/servo/libexpat/issues/30
License:        MIT
URL:            https://crates.io/crates/expat-sys
Source:         %{crates_source}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
XML parser library written in C.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Requires:       pkgconfig(expat) >= 2.1.0

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
# remove bundled copy of expat
rm -vr expat/
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires
echo 'pkgconfig(expat) >= 2.1.0'

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
