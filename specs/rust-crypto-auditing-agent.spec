# Generated by rust2rpm 28
# * Tests can only be run in the workspace
%bcond check 0

%global crate crypto-auditing-agent

Name:           rust-crypto-auditing-agent
Version:        0.3.0
Release:        %autorelease
Summary:        Event collector agent for crypto-auditing project

License:        GPL-3.0-or-later
URL:            https://crates.io/crates/crypto-auditing-agent
Source:         %{crates_source}
Source2:        agent.conf
Source3:        crau-agent.service
Source4:        crypto-auditing.sysusers
# Manually created patch for downstream crate metadata changes
# * Remove unused direct dependency on nix:
#   https://github.com/latchset/crypto-auditing/pull/257
Patch:          crypto-auditing-agent-fix-metadata.diff
Patch10:        rust-crypto-auditing-agent-0.2.4-no-probe.patch

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  kernel-devel
BuildRequires:  libbpf-devel
BuildRequires:  bpftool
BuildRequires:  systemd-rpm-macros

# Neither kernel-devel nor kernel-tools (bpftool) supports 32-bit arches
ExcludeArch: i386 i686

%global _description %{expand:
Event collector agent for crypto-auditing project.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# Upstream license specification: GPL-3.0-or-later
#
# The build dependencies have the following licenses:
#
#   (MIT OR Apache-2.0) AND Unicode-DFS-2016
#   Apache-2.0
#   Apache-2.0 OR MIT
#   BSD-2-Clause
#   GPL-3.0-or-later
#   LGPL-2.1-only OR BSD-2-Clause
#   MIT
#   MIT OR Apache-2.0
#   Unlicense OR MIT
#
License:        %{shrink:
    GPL-3.0-or-later AND
    Apache-2.0 AND
    BSD-2-Clause AND
    MIT AND
    Unicode-DFS-2016 AND
    (Apache-2.0 OR MIT) AND
    (LGPL-2.1-only OR BSD-2-Clause) AND
    (Unlicense OR MIT)
    }
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.GPL-2.0
%license LICENSE.LGPL-2.1
%license LICENSE.dependencies
%doc README.md
%{_bindir}/crau-agent
%attr(0755,root,root) %dir %{_sysconfdir}/crypto-auditing
%config(noreplace) %{_sysconfdir}/crypto-auditing/agent.conf
%dir %{_sysconfdir}/crypto-auditing/
%{_unitdir}/crau-agent.service
%{_sysusersdir}/crypto-auditing.conf
%attr(0755,crypto-auditing,crypto-auditing) %dir %{_localstatedir}/log/crypto-auditing

%post -n %{crate}
%systemd_post crau-agent.service

%preun -n %{crate}
%systemd_preun crau-agent.service

%postun -n %{crate}
%systemd_postun_with_restart crau-agent.service

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# Use vmlinux.h from kernel-devel, instead of the one extracted with bpftool",
cp $(rpm -ql kernel-devel | grep '/vmlinux.h$' | tail -1) src/bpf

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
mkdir -p $RPM_BUILD_ROOT%{_sysconfdir}/crypto-auditing
install -p -m 644 %{SOURCE2} $RPM_BUILD_ROOT%{_sysconfdir}/crypto-auditing

mkdir -p $RPM_BUILD_ROOT%{_unitdir}
install -p -m 644 %{SOURCE3} $RPM_BUILD_ROOT%{_unitdir}

mkdir -p $RPM_BUILD_ROOT%{_sysusersdir}
install -p -m 644 %{SOURCE4} $RPM_BUILD_ROOT%{_sysusersdir}/crypto-auditing.conf

mkdir -p $RPM_BUILD_ROOT%{_localstatedir}/log/crypto-auditing

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
