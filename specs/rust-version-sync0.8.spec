# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate version-sync

Name:           rust-version-sync0.8
Version:        0.8.1
Release:        %autorelease
Summary:        Simple crate for ensuring that version numbers in README files are updated

License:        MIT
URL:            https://crates.io/crates/version-sync
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump pulldown-cmark from 0.4 to 0.8
Patch:          version-sync-fix-metadata.diff
# * fix tests with toml 0.5.3:
#   https://github.com/mgeisler/version-sync/commit/33dc77f
Patch:          0001-markdown_deps-handle-test-output-from-toml-0.5.3.patch
# * port to latest pulldown-cmark
Patch:          0001-chore-Update-pulldown-cmark-to-0.7.patch

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Simple crate for ensuring that version numbers in README files are
updated when the crate version changes.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
