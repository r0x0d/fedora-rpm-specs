# Generated by rust2rpm 18
# Remove doctests because of missing files
# https://github.com/mozilla/mp4parse-rust/issues/315
%bcond_without check
%global debug_package %{nil}

%global crate mp4parse

Name:           rust-%{crate}
Version:        0.12.0
Release:        %autorelease
Summary:        Parser for ISO base media file format (mp4)

# Upstream license specification: MPL-2.0
License:        MPLv2.0
URL:            https://crates.io/crates/mp4parse
Source:         %{crates_source}

ExclusiveArch:  %{rust_arches}
%if %{__cargo_skip_build}
BuildArch:      noarch
%endif

BuildRequires:  rust-packaging

%global _description %{expand:
Parser for ISO base media file format (mp4).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages
which use "%{crate}" crate.

%files          devel
%license LICENSE
%doc README.md
%{cargo_registry}/%{crate}-%{version_no_tilde}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages
which use "default" feature of "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+3gpp-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+3gpp-devel %{_description}

This package contains library source intended for building other packages
which use "3gpp" feature of "%{crate}" crate.

%files       -n %{name}+3gpp-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+meta-xml-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+meta-xml-devel %{_description}

This package contains library source intended for building other packages
which use "meta-xml" feature of "%{crate}" crate.

%files       -n %{name}+meta-xml-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+missing-pixi-permitted-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+missing-pixi-permitted-devel %{_description}

This package contains library source intended for building other packages
which use "missing-pixi-permitted" feature of "%{crate}" crate.

%files       -n %{name}+missing-pixi-permitted-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+mp4v-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+mp4v-devel %{_description}

This package contains library source intended for building other packages
which use "mp4v" feature of "%{crate}" crate.

%files       -n %{name}+mp4v-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%package     -n %{name}+unstable-api-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-api-devel %{_description}

This package contains library source intended for building other packages
which use "unstable-api" feature of "%{crate}" crate.

%files       -n %{name}+unstable-api-devel
%ghost %{cargo_registry}/%{crate}-%{version_no_tilde}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test -- --lib
%endif

%changelog
%autochangelog
