# Generated by rust2rpm 26
# * missing optional and dev-dependencies
%bcond_with check
%global debug_package %{nil}

%global crate firestorm

Name:           rust-firestorm
Version:        0.5.1
Release:        %autorelease
Summary:        Low overhead intrusive flamegraph profiler

License:        MIT
URL:            https://crates.io/crates/firestorm
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop features with missing dependencies
# * drop unused, benchmark-only criterion dev-dependency
# * remove reference to readme file that is not included in published crates
Patch:          firestorm-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A low overhead intrusive flamegraph profiler.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
