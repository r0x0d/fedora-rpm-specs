# Generated by rust2rpm 22
%bcond_without check
%global debug_package %{nil}

%global crate wezterm-dynamic

Name:           rust-wezterm-dynamic
Version:        0.1.0
Release:        %autorelease
Summary:        Config serialization for wezterm via dynamic json-like data values

License:        MIT
URL:            https://crates.io/crates/wezterm-dynamic
Source:         %{crates_source}
# https://github.com/wez/wezterm/pull/2418
Source:         https://github.com/wez/wezterm/raw/65d482a3f759604c79597f6d5a6a0adb98914461/wezterm-dynamic/LICENSE.md

ExclusiveArch:  %{rust_arches}

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Config serialization for wezterm via dynamic json-like data values.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp -p %SOURCE1 .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
