# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/noborus/guesswidth
%global goipath         github.com/noborus/guesswidth
Version:                0.4.0

%gometa -L -f

%global common_description %{expand:
Guess the width (fwf) output without delimiters in commands.}

%global golicenses      LICENSE
%global godocs          docs README.md

Name:           golang-github-noborus-guesswidth
Release:        %autorelease
Summary:        Guess the width output without delimiters in commands

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
