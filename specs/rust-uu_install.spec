# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

# prevent executables from being installed
%global cargo_install_bin 0

%global crate uu_install

Name:           rust-uu_install
Version:        0.7.0
Release:        %autorelease
Summary:        install - (uutils) copy files from SOURCE to DESTINATION

License:        MIT
URL:            https://crates.io/crates/uu_install
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * loosen selinux version bound from =0.6.0 to 0.6.0
Patch:          uu_install-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 26

%global _description %{expand:
install - (uutils) copy files from SOURCE to DESTINATION (with specified attributes).}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.package.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+selinux-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+selinux-devel %{_description}

This package contains library source intended for building other packages which
use the "selinux" feature of the "%{crate}" crate.

%files       -n %{name}+selinux-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
