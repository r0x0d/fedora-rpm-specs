# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate exitfailure

Name:           rust-exitfailure
Version:        0.5.1
Release:        %autorelease
Summary:        Basic newtype wrappers for use with ? in main

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/exitfailure
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# Bump assert_cmd to 2.0 and predicates to 1.0
# https://github.com/tismith/exitfailure/pull/38
# https://github.com/tismith/exitfailure/pull/41
Patch:          exitfailure-fix-metadata.diff

BuildRequires:  rust-packaging >= 21
BuildRequires:  sed

%global _description %{expand:
A basic newtype wrappers for use with ? in main.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
# This test hardcodes target/debug to shell out to the examples, which won't
# work as we run the tests in release mode
sed -i 's:target/debug/:target/release/:g' tests/test.rs

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# https://github.com/tismith/exitfailure/issues/42
%cargo_test -- -- --skip test_backtrace
%endif

%changelog
%autochangelog
