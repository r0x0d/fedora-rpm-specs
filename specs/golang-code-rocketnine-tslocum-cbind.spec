# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://code.rocket9labs.com/tslocum/cbind
# https://code.rocket9labs.com/tslocum/cbind/archive/v0.1.5.tar.gz
%global goipath         code.rocketnine.space/tslocum/cbind
%global forgeurl        https://code.rocket9labs.com/tslocum/cbind
Version:                0.1.5
%global repo            cbind
%global archivename     %{repo}-%{version}
%global archiveext      tar.gz
%global archiveurl      %{forgeurl}/archive/v%{version}.tar.gz
%global topdir          %{repo}
%global extractdir      %{repo}
%global scm             git

%gometa -L -f

%global common_description %{expand:
Key event handling library for tcell.}

%global golicenses      LICENSE
%global godocs          CHANGELOG README.md

Name:           golang-code-rocketnine-tslocum-cbind
Release:        %autorelease
Summary:        Key event handling library for tcell

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
