# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate numpy

Name:           rust-numpy
Version:        0.27.1
Release:        %autorelease
Summary:        PyO3-based Rust bindings of the NumPy C-API

License:        BSD-2-Clause
URL:            https://crates.io/crates/numpy
Source:         %{crates_source}
# * Fix tests under Python 3.14
# * Commenting out the docs hunk that doesn't apply cleanly
Patch2:         https://patch-diff.githubusercontent.com/raw/PyO3/rust-numpy/pull/524.patch

BuildRequires:  cargo-rpm-macros >= 24
%if %{with check}
BuildRequires:  python3dist(numpy)
%endif

%global _description %{expand:
PyO3-based Rust bindings of the NumPy C-API.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+half-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+half-devel %{_description}

This package contains library source intended for building other packages which
use the "half" feature of the "%{crate}" crate.

%files       -n %{name}+half-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+nalgebra-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+nalgebra-devel %{_description}

This package contains library source intended for building other packages which
use the "nalgebra" feature of the "%{crate}" crate.

%files       -n %{name}+nalgebra-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%if 0%{?__isa_bits} == 32
# * Tests dtype::tests::test_dtype_methods_* fail on 32-bit (but not 32-bit
#   Windows, per upstream CI) because a few sizes and alignments are different
#   from the hard-coded expected values. We explored fixing this in
#   https://github.com/PyO3/rust-numpy/pull/511 but it’s just not worth it: we
#   skip the affected tests instead.
%cargo_test -- -- --skip dtype::tests::test_dtype_methods_
%else
%cargo_test
%endif
%endif

%changelog
%autochangelog
