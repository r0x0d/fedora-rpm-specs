# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/gofrs/uuid
%global goipath         github.com/gofrs/uuid/v5
Version:                5.3.0

%gometa -L -f

%global common_description %{expand:
A UUID package for Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-gofrs-uuid5
Release:        %autorelease
Summary:        A UUID package for Go

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
