# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

# https://github.com/ettle/strcase
%global goipath         github.com/ettle/strcase
Version:                0.2.0

%gometa -L

%global common_description %{expand:
Go package to convert strings to snake_case, camelCase, kebab-case and more!
Supports Go initialisms.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-ettle-strcase
Release:        %autorelease
Summary:        Go package to convert strings to snake_case, camelCase, kebab-case

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
