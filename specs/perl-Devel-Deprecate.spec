Name:           perl-Devel-Deprecate
Version:        0.01
Release:        1%{?dist}
Summary:        Create deprecation schedules in your code
# lib/Devel/Deprecate.pm
License:        GPL-1.0-or-later OR Artistic-1.0-Perl
URL:            https://metacpan.org/dist/Devel-Deprecate
Source0:        https://www.cpan.org/authors/id/O/OV/OVID/Devel-Deprecate-%{version}.tar.gz
BuildArch:      noarch

# build requirements
BuildRequires:  coreutils
BuildRequires:  make
BuildRequires:  perl-generators
BuildRequires:  perl-interpreter
BuildRequires:  perl(ExtUtils::MakeMaker) >= 6.76
# runtime requirements
BuildRequires:  perl(Carp)
BuildRequires:  perl(DateTime) >= 0.3
BuildRequires:  perl(Exporter)
BuildRequires:  perl(Scalar::Util) >= 1.14
BuildRequires:  perl(base)
BuildRequires:  perl(strict)
BuildRequires:  perl(vars)
BuildRequires:  perl(warnings)
# test requirements
BuildRequires:  perl(Data::Dumper)
BuildRequires:  perl(Test::More)

%description
Many times we find ourselves needing to deprecate code or have a deadline
and just don't have time to refactor. Instead of trying to remember about
this, posting it to a wiki or sending an email, it's better to have an
automatic way to deprecate something. This module allows you to do that and
embeds the deprecation directly in the code you wish to deprecate.

%prep
%setup -q -n Devel-Deprecate-%{version}

%build
perl Makefile.PL INSTALLDIRS=vendor NO_PACKLIST=1 NO_PERLLOCAL=1
%{make_build}

%install
%{make_install}
%{_fixperms} $RPM_BUILD_ROOT/*

%check
%{make_build} test

%files
%doc Changes README
%{perl_vendorlib}/Devel*
%{_mandir}/man3/Devel*

%changelog
* Mon Aug 10 2026 Emmanuel Seyman <emmanuel@seyman.fr> - 0.01-1
- Initial specfile, based on the one autogenerated by cpanspec 1.78.
