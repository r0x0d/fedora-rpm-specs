# Generated by rust2rpm 28
# * tests circularly require trustfall
%bcond check 0
%global debug_package %{nil}

%global crate trustfall_derive

Name:           rust-trustfall_derive
Version:        0.3.1
Release:        %autorelease
Summary:        Derive macros for the trustfall query engine

License:        Apache-2.0
URL:            https://crates.io/crates/trustfall_derive
Source:         %{crates_source}
# * add license: https://github.com/obi1kenobi/trustfall/pull/734
Source2:        https://raw.githubusercontent.com/obi1kenobi/trustfall/refs/tags/trustfall-v0.8.0/LICENSE.txt

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Derive macros for the trustfall query engine.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# copy in license text
cp -p %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
