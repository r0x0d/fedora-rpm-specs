# Generated by rust2rpm 26
# * tests require unreleased version of concat-ident
%bcond_with check
%global debug_package %{nil}

%global crate human-date-parser

Name:           rust-human-date-parser
Version:        0.1.2
Release:        %autorelease
Summary:        Parses strings that express dates in a human way into ones usable by code

License:        MIT
URL:            https://crates.io/crates/human-date-parser
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Parses strings that express dates in a human way into ones usable by
code.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/Readme.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
# cargo seems to have problems excluding this (possibly because of the space)
# so just delete it here
rm Missing\ phrases.md
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
