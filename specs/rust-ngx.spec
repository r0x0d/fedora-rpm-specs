# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate ngx

Name:           rust-ngx
Version:        0.5.1
Release:        %autorelease
Summary:        FFI bindings to NGINX

License:        Apache-2.0
URL:            https://crates.io/crates/ngx
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Disable feature "vendored"
# * Exclude _nginx_buildsrcdir/LICENSE from the package
# * Remove unused test dependencies
Patch:          ngx-fix-metadata.diff
# * Downgrade allocator-api2 dependency to 0.2.x
Patch2:         0001-Revert-chore-update-allocator-api2-to-0.3.1.patch

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  nginx-mod-devel

%global _description %{expand:
FFI bindings to NGINX.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/CODE_OF_CONDUCT.md
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/README.md
%doc %{crate_instdir}/SECURITY.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+alloc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+alloc-devel %{_description}

This package contains library source intended for building other packages which
use the "alloc" feature of the "%{crate}" crate.

%files       -n %{name}+alloc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+async-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+async-devel %{_description}

This package contains library source intended for building other packages which
use the "async" feature of the "%{crate}" crate.

%files       -n %{name}+async-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
# generate headers for bindgen
touch config
%nginx_modconfigure
export NGINX_BUILD_DIR=$(realpath %{_vpath_builddir})
export NGINX_SOURCE_DIR=$(realpath %{_nginx_buildsrcdir})
%cargo_build

%install
%cargo_install

%if %{with check}
%check
export NGINX_BUILD_DIR=$(realpath %{_vpath_builddir})
export NGINX_SOURCE_DIR=$(realpath %{_nginx_buildsrcdir})
%cargo_test
%endif

%changelog
%autochangelog
