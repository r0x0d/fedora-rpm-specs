# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate chic

Name:           rust-chic
Version:        1.2.2
Release:        %autorelease
Summary:        Pretty parser error reporting

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/chic
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * bump annotate-snippets dependency from 0.6 to 0.11
Patch:          chic-fix-metadata.diff
# * port from annotate-snippets 0.6 to 0.9:
#   https://github.com/yoshuawuyts/chic/pull/2
Patch:          0001-port-from-annotate-snippets-0.6-to-0.9.patch
# * port from annotate-snippets 0.9 to 0.10 and from 0.10 to 0.11:
#   https://salsa.debian.org/rust-team/debcargo-conf/-/tree/master/src/chic/
Patch:          0002-port-from-annotate-snippets-0.9-to-0.10.patch
Patch:          0003-port-from-annotate-snippets-0.10-to-0.11.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Pretty parser error reporting.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
