# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate open-enum-derive

Name:           rust-open-enum-derive
Version:        0.5.2
Release:        %autorelease
Summary:        Attribute for generating "open" fieldless enums that accept any integer value

License:        Apache-2.0
URL:            https://crates.io/crates/open-enum-derive
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An attribute for generating "open" C-like enums, those that accept any
integer value, by using a newtype struct and associated constants.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+repr_c-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+repr_c-devel %{_description}

This package contains library source intended for building other packages which
use the "repr_c" feature of the "%{crate}" crate.

%files       -n %{name}+repr_c-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
