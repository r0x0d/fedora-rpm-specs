# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

# prevent executables from being installed
%global cargo_install_bin 0

%global crate uu_seq

Name:           rust-uu_seq
Version:        0.7.0
Release:        %autorelease
Summary:        seq ~ (uutils) display a sequence of numbers

License:        MIT
URL:            https://crates.io/crates/uu_seq
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop benchmarking dependencies like divan
Patch:          uu_seq-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 26

%global _description %{expand:
seq ~ (uutils) display a sequence of numbers.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/BENCHMARKING.md
%doc %{crate_instdir}/README.package.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip one test that fails due to minor formatting differences between
#   bigdecimal 0.4.8 and earlier versions
%cargo_test # -- -- --exact --skip extendedbigdecimal::tests::test_display
%endif

%changelog
%autochangelog
