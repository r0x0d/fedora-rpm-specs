# Generated by rust2rpm 28
# * tests required not packaged benchmark dependency
%bcond check 0
%global debug_package %{nil}

%global crate gix-imara-diff

Name:           rust-gix-imara-diff
Version:        0.2.3
Release:        %autorelease
Summary:        Gitoxide fork of imara-diff

License:        Apache-2.0
URL:            https://crates.io/crates/gix-imara-diff
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * remove benchmark dependency (cov-mark)
Patch:          gix-imara-diff-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A high performance library for computing diffs, maintained as a modified
copy of upstream imara-diff for gitoxide.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unified_diff-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unified_diff-devel %{_description}

This package contains library source intended for building other packages which
use the "unified_diff" feature of the "%{crate}" crate.

%files       -n %{name}+unified_diff-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
