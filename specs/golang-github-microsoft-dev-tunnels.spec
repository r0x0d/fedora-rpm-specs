# Generated by go2rpm 1.9.0
%bcond_without check
%global debug_package %{nil}

# https://github.com/microsoft/dev-tunnels
%global goipath         github.com/microsoft/dev-tunnels
Version:                0.0.25

%gometa -f


%global common_description %{expand:
Dev tunnels allows developers to securely expose local web services to the
Internet, control who has access, and easily & debug your web applications
from anywhere.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Dev Tunnels SDK

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%install
%gopkginstall

%if %{with check}
%check
# These tests require Internet connection
for test in "TestTunnelCreateDelete" "TestListTunnels" "TestTunnelCreateUpdateDelete" \
            "TestTunnelCreateUpdateTwiceDelete" "TestTunnelCreateGetDelete" \
            "TestTunnelAddPort" "TestTunnelDeletePort" "TestTunnelUpdatePort" \
            "TestTunnelListPorts" "TestTunnelEndpoints" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
