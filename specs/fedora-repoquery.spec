# generated by cabal-rpm-2.2.2
# https://docs.fedoraproject.org/en-US/packaging-guidelines/Haskell/

%global shortname frpq

# requires network for dnf
%bcond_with tests

Name:           fedora-repoquery
Version:        0.7.1
Release:        %autorelease
Summary:        Fedora release repos package query tool

License:        GPL-3.0-or-later
Url:            https://hackage.haskell.org/package/fedora-repoquery
# Begin cabal-rpm sources:
Source0:        https://hackage.haskell.org/package/%{name}-%{version}/%{name}-%{version}.tar.gz
# End cabal-rpm sources

# Begin cabal-rpm deps:
BuildRequires:  ghc-Cabal-devel
BuildRequires:  ghc-rpm-macros
BuildRequires:  ghc-Glob-devel
BuildRequires:  ghc-base-devel
BuildRequires:  ghc-bodhi-devel
BuildRequires:  ghc-cached-json-file-devel
BuildRequires:  ghc-case-insensitive-devel
BuildRequires:  ghc-curl-devel
BuildRequires:  ghc-directory-devel
BuildRequires:  ghc-extra-devel
BuildRequires:  ghc-filepath-devel
BuildRequires:  ghc-regex-compat-devel
BuildRequires:  ghc-safe-devel
BuildRequires:  ghc-simple-cmd-devel
BuildRequires:  ghc-simple-cmd-args-devel
BuildRequires:  ghc-text-devel
BuildRequires:  ghc-time-devel
BuildRequires:  ghc-xdg-basedir-devel
BuildRequires:  help2man
# End cabal-rpm deps

%description
A CLI tool for repoquerying Fedora and Centos Stream release packages:
by default it displays their version and repo location and
optionally repo compose timestamps.

If you want a short alias then also install %{name}-short.


%package short
Summary:        Provides %{shortname} alias for fedora-repoquery
Requires:       %{name} = %{version}-%{release}
BuildArch:      noarch

%description short
The package provides a shorter alias symlink for fedora-repoquery: %{shortname}


%prep
# Begin cabal-rpm setup:
%setup -q
# End cabal-rpm setup


%build
# Begin cabal-rpm build:
%ghc_bin_build
# End cabal-rpm build


%install
# Begin cabal-rpm install
%ghc_bin_install

set noclobber
mkdir -p %{buildroot}%{bash_completions_dir}
%{buildroot}%{_bindir}/%{name} --bash-completion-script %{name} | sed s/filenames/default/ > %{buildroot}%{bash_completions_dir}/%{name}

mkdir -p %{buildroot}%{_mandir}/man1/
help2man --no-info %{buildroot}%{_bindir}/%{name} > %{buildroot}%{_mandir}/man1/%{name}.1
# End cabal-rpm install

ln -s %{name} %{buildroot}%{_bindir}/%{shortname}
ln -s %{name}.1 %{buildroot}%{_mandir}/man1/%{shortname}.1


%check
%if %{with tests}
PATH=%{buildroot}%{_bindir}:$PATH
%cabal_test
%endif


%files
# Begin cabal-rpm files:
%license COPYING
%doc ChangeLog.md README.md
%{_bindir}/%{name}
%{bash_completions_dir}/%{name}
%{_mandir}/man1/%{name}.1*
# End cabal-rpm files


%files short
%{_bindir}/%{shortname}
%{_mandir}/man1/%{shortname}.1*


%changelog
%autochangelog
