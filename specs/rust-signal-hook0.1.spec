# Generated by rust2rpm 26
# * outdated dev-dependencies: version-sync ^0.8, tokio ^0.1
%bcond_with check
%global debug_package %{nil}

%global crate signal-hook

Name:           rust-signal-hook0.1
Version:        0.1.17
Release:        %autorelease
Summary:        Unix signal handling

# Upstream license specification: Apache-2.0/MIT
License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/signal-hook
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop unused support for tokio ^0.1 and mio ^0.6
Patch:          signal-hook-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Unix signal handling.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+mio-0_7-support-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+mio-0_7-support-devel %{_description}

This package contains library source intended for building other packages which
use the "mio-0_7-support" feature of the "%{crate}" crate.

%files       -n %{name}+mio-0_7-support-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+mio_0_7-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+mio_0_7-devel %{_description}

This package contains library source intended for building other packages which
use the "mio_0_7" feature of the "%{crate}" crate.

%files       -n %{name}+mio_0_7-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
