# Generated by go2rpm 1.18.0
%bcond check 1

# https://github.com/kubernetes-sigs/kind
%global goipath         github.com/kubernetes-sigs/kind
Version:                0.30.0

%gometa -L -f


Name:           kind
Release:        %autorelease
Summary:        Kubernetes IN Docker - local clusters for testing Kubernetes

# Generated by go-vendor-tools
License:        Apache-2.0 AND BSD-2-Clause AND BSD-3-Clause AND CC-BY-4.0 AND MIT
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

BuildRequires:  go-vendor-tools

Requires:       (docker-cli or podman-docker)

%description
Kubernetes IN Docker - local clusters for testing Kubernetes.

%prep
%goprep -A
%setup -q -T -D -a1 %{forgesetupargs}

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
%global gomodulesmode GO111MODULE=on
%gobuild -o %{gobuilddir}/bin/%{name} main.go

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
# TestIntegrationEnsureNetworkConcurrent requires docker daemon
for test in "TestIntegrationEnsureNetworkConcurrent"; do
  awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%gotest ./...
%endif

%files -f %{go_vendor_license_filelist}
%license vendor/modules.txt
%doc CONTRIBUTING.md README.md code-of-conduct.md
%{_bindir}/kind


%changelog
%autochangelog
