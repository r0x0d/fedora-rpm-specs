# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate datetime

Name:           rust-datetime
Version:        0.5.2
Release:        %autorelease
Summary:        Library for date and time formatting and arithmetic

License:        MIT
URL:            https://crates.io/crates/datetime
Source:         %{crates_source}
# Automatically generated patch to strip foreign dependencies
Patch:          datetime-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * bump iso8601 dependency from 0.3 to 0.4:
#   https://github.com/rust-datetime/datetime/pull/30
Patch:          datetime-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Library for date and time formatting and arithmetic.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENCE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+format-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+format-devel %{_description}

This package contains library source intended for building other packages which
use the "format" feature of the "%{crate}" crate.

%files       -n %{name}+format-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+iso8601-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+iso8601-devel %{_description}

This package contains library source intended for building other packages which
use the "iso8601" feature of the "%{crate}" crate.

%files       -n %{name}+iso8601-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+locale-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+locale-devel %{_description}

This package contains library source intended for building other packages which
use the "locale" feature of the "%{crate}" crate.

%files       -n %{name}+locale-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+pad-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+pad-devel %{_description}

This package contains library source intended for building other packages which
use the "pad" feature of the "%{crate}" crate.

%files       -n %{name}+pad-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+parse-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+parse-devel %{_description}

This package contains library source intended for building other packages which
use the "parse" feature of the "%{crate}" crate.

%files       -n %{name}+parse-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
# exclude executable file that causes an unnecessary ruby dependency
rm tests/generate_tests.rb
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
