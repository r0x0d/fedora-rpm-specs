# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%if %{with bootstrap}
%global debug_package %{nil}
%endif

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/evilsocket/shellz
%global goipath         github.com/evilsocket/shellz
Version:                1.6.0

%gometa -L -f

%global common_description %{expand:
Shellz is a small utility to manage your ssh, telnet, kubernetes, winrm, web or
any custom shell in a single place.}

%global golicenses      LICENSE.md
%global godocs          README.md

Name:           shellz
Release:        %autorelease
Summary:        Utility to tracking and controlling shells and tunnels

License:        GPL-3.0-only
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%if %{without bootstrap}
%build
%gobuild -o %{gobuilddir}/bin/%{name} %{goipath}/cmd/%{name}
%endif

%install
%if %{without bootstrap}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/
%endif

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%if %{without bootstrap}
%files
%license LICENSE.md
%doc README.md
%{_bindir}/shellz
%endif

%changelog
%autochangelog
