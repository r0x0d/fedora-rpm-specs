# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate reqsign-google

Name:           rust-reqsign-google
Version:        3.0.0
Release:        %autorelease
Summary:        Google Cloud Platform signing implementation for reqsign

License:        Apache-2.0
URL:            https://crates.io/crates/reqsign-google
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Until we have rust-jsonwebtoken 10.x,
#   https://bugzilla.redhat.com/show_bug.cgi?id=2400251, use 9.x, and remove the
#   crypto backend feature dependency since these were not introduced yet. Once we
#   have 10.x, then (assuming aws-lc is not yet packaged) we will need to replace
#   the aws_lc_rs feature dependency with the rust_crypto dependency.
Patch:          reqsign-google-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Google Cloud Platform signing implementation for reqsign.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
# Remove unused mock server scripts from tests to avoid a Python dependency
rm -rv tests/mocks
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
