# Generated by rust2rpm 28
# * tests can only be run in-tree
%bcond check 0
%global debug_package %{nil}

%global crate slog-extlog-derive

Name:           rust-slog-extlog-derive
Version:        8.1.0
Release:        %autorelease
Summary:        Custom derive code for slog-extlog

License:        Apache-2.0
URL:            https://crates.io/crates/slog-extlog-derive
Source:         %{crates_source}
# * https://github.com/slog-rs/extlog/issues/34
Source2:        https://github.com/slog-rs/extlog/raw/refs/tags/8.1.0/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Custom derive code for slog-extlog.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav '%{SOURCE2}' .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
