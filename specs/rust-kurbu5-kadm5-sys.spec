# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate kurbu5-kadm5-sys

Name:           rust-kurbu5-kadm5-sys
Version:        0.1.2
Release:        %autorelease
Summary:        KADM5 plugin API bindings

License:        BSD-2-Clause
URL:            https://crates.io/crates/kurbu5-kadm5-sys
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  krb5-devel

%global _description %{expand:
KADM5 plugin API bindings — links libkadm5srv_mit and re-exports
kurbu5-sys types.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/
%exclude %{crate_instdir}/rust2rpm.toml
%exclude %{crate_instdir}/rust-kurbu5-rs.spec

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
