# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate tagptr

Name:           rust-tagptr
Version:        0.2.0
Release:        %autorelease
Summary:        Strongly typed atomic and non-atomic tagged pointers

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/tagptr
Source:         %{crates_source}
# * Missing license text, https://github.com/oliver-giersch/tagptr/pull/3
Source2:        https://github.com/oliver-giersch/tagptr/raw/81e5c6a5cbd86e0da1e47deb9ce134d1e5624137/LICENSE-APACHE
# * Assertion “tag bits in raw pointer must be zeroed” fails on i686 (32-bit
#   x86) with Rust 1.93 and later
# * https://github.com/oliver-giersch/tagptr/issues/5
# * This appears to be a real and serious bug, but propagating ExcludeArch
#   through this package’s reverse dependency tree would be unpleasant, the
#   direct reverse dependency rust-moka still builds and passes its tests, and
#   any latent bugs probably don’t affect anyone in practice given the fact that
#   most 32-bit multilib packages are not used by anyone at all. Therefore,
#   without much in the way of better options at hand, we choose to simply cover
#   up the problem.
Patch10:        tagptr-0.2.0-32bit-bug.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Strongly typed atomic and non-atomic tagged pointers.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp -p '%{SOURCE2}' .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
