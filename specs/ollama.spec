# Generated by go2rpm 1.19.0
%bcond check 1

# https://github.com/ollama/ollama
%global goipath         github.com/ollama/ollama
Version:                0.19.0

%gometa -L -f


Name:           ollama
Release:        %autorelease
Summary:        Get up and running with Kimi-K2.5, GLM-5, MiniMax, DeepSeek, gpt-oss, Qwen, Gemma and other models

# Generated by go-vendor-tools
License:        Apache-2.0 AND BSD-2-Clause AND BSD-3-Clause AND BSD-3-Clause-HP AND BSL-1.0 AND CC-BY-3.0 AND CC-BY-4.0 AND CC0-1.0 AND ISC AND LicenseRef-Fedora-Public-Domain AND LicenseRef-scancode-protobuf AND MIT AND NCSA AND NTP AND OpenSSL AND ZPL-2.1 AND Zlib
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

BuildRequires:  go-vendor-tools

%description
Get up and running with Kimi-K2.5, GLM-5, MiniMax, DeepSeek, gpt-oss, Qwen,
Gemma and other models.

%prep
%goprep -p1
tar -xf %{S:1}

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
%global gomodulesmode GO111MODULE=on
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done
%gobuild -o %{gobuilddir}/bin/ollama %{goipath}
for cmd in docs/tools/extract-examples x/imagegen/mlx x/mlxrunner/mlx/generator; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
%gotest ./...
%endif

%files -f %{go_vendor_license_filelist}
%doc docs CONTRIBUTING.md README.md SECURITY.md examples app/README.md
%doc app/webview/README.md cmd/bench/README.md integration/README.md
%doc llama/README.md runner/README.md docs x/imagegen/README.md
%doc x/imagegen/cmd/engine/README.md x/imagegen/mlx/README.md
%doc x/imagegen/tokenizer/README.md x/mlxrunner/mlx/include/mlx/c/README.md
%{_bindir}/ollama


%changelog
%autochangelog
