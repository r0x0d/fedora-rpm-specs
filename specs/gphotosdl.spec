# Generated by go2rpm 1.17.1
%bcond check 1

# https://github.com/rclone/gphotosdl
%global goipath         github.com/rclone/gphotosdl
Version:                0.0.2

%gometa -L -f


Name:           gphotosdl
Release:        %autorelease
Summary:        This is a original resolution Google Photos downloader for use with rclone

# Generated by go-vendor-tools
License:        MIT
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

BuildRequires:  go-vendor-tools

%description
This is a original resolution Google Photos downloader for use with rclone.

%prep
%goprep -A
%setup -q -T -D -a1 %{forgesetupargs}
%autopatch -p1

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
%global gomodulesmode GO111MODULE=on
%gobuild -o %{gobuilddir}/bin/gphotosdl %{goipath}

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
%gotest ./...
%endif

%files -f %{go_vendor_license_filelist}
%license vendor/modules.txt
%doc README.md RELEASE.md
%{_bindir}/gphotosdl


%changelog
%autochangelog
