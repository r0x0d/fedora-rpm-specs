# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate tree-sitter-commonlisp

Name:           rust-tree-sitter-commonlisp
Version:        0.4.1
Release:        %autorelease
Summary:        Common Lisp grammar for the tree-sitter parsing library

License:        MIT
URL:            https://crates.io/crates/tree-sitter-commonlisp
Source:         %{crates_source}
# * add license:
#   https://github.com/tree-sitter-grammars/tree-sitter-commonlisp/pull/51
Source2:        https://raw.githubusercontent.com/tree-sitter-grammars/tree-sitter-commonlisp/refs/tags/v%{version}/LICENSE.md
# Manually created patch for downstream crate metadata changes
# * include LICENSE.md
Patch:          tree-sitter-commonlisp-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Common Lisp grammar for the tree-sitter parsing library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# copy in license text
cp -p %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
