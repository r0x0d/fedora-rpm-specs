# Generated by go2rpm 1.5.0
%bcond_without check

%global debug_package %{nil}

# https://github.com/docker-slim/uilive
%global goipath         github.com/docker-slim/uilive
Version:                0.0.2
%global commit          c7c9d1c0f8e4eb82b5e6bb63f6054ec0202fc822

%gometa

%global common_description %{expand:
Uilive is a go library for updating terminal output in realtime.}

%global golicenses      LICENSE
%global godocs          doc example README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Uilive is a go library for updating terminal output in realtime

License:        MIT
URL:            %{gourl}
Source0:        %{gosource}

BuildRequires:  golang(github.com/gosuri/uilive)

%description
%{common_description}

%gopkg

%prep
%goprep
sed -i 's|"github.com/gosuri/uilive|"github.com/docker-slim/uilive|' $(find . -iname "*.go" -type f)

%install
%gopkginstall

%if %{with check}
%check
for test in "TestWriter" \
; do
awk -i inplace '/^func.*'"$test"'\(/ { print; print "\tt.Skip(\"disabled failing test\")"; next}1' $(grep -rl $test)
done
%gocheck
%endif

%gopkgfiles

%changelog
%autochangelog
