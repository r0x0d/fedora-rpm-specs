# Generated by rust2rpm 28
%bcond check 1

%global crate zlink-codegen

Name:           rust-zlink-codegen
Version:        0.7.0
Release:        %autorelease
Summary:        Utility to generate zlink code from varlink IDL files

License:        MIT
URL:            https://crates.io/crates/zlink-codegen
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Utility to generate zlink code from varlink IDL files.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
License:        %{shrink:
                    (Apache-2.0 OR BSL-1.0) AND
                    (Apache-2.0 OR MIT) AND
                    (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND
                    MIT AND
                    (Unlicense OR MIT)
                }
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc CHANGELOG.md
%doc README.md
%{_bindir}/zlink-codegen

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
# * Missing file camelcase.idl
# * The doc tests only work in the git repoitory as they reference files from a
#   book directory.
%cargo_test -- --bins -- --skip test_camelcase_rename_attributes
%cargo_test -- --lib -- --skip test_camelcase_rename_attributes
%endif

%changelog
%autochangelog
