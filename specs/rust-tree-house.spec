# Generated by rust2rpm 27
# * tests require path dependency which is not published to crates.io
%bcond check 0
%global debug_package %{nil}

%global crate tree-house

Name:           rust-tree-house
Version:        0.3.0
Release:        %autorelease
Summary:        Robust and cozy highlighter library for tree-sitter

License:        MPL-2.0
URL:            https://crates.io/crates/tree-house
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * remove not used dev-dependency cov-mark
#   (https://github.com/helix-editor/tree-house/pull/23)
# * allow version 0.1.14 for unicode-width
Patch:          tree-house-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A robust and cozy highlighter library for tree-sitter.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+fixtures-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+fixtures-devel %{_description}

This package contains library source intended for building other packages which
use the "fixtures" feature of the "%{crate}" crate.

%files       -n %{name}+fixtures-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
