# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate macrotest

Name:           rust-macrotest
Version:        1.2.1
Release:        %autorelease
Summary:        Test harness for macro expansion

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/macrotest
Source:         %{crates_source}
# * add license: https://github.com/eupn/macrotest/pull/135
Source2:        https://raw.githubusercontent.com/michel-slm/macrotest/refs/heads/add-license/LICENSE-MIT
# * add license
Source3:        https://raw.githubusercontent.com/michel-slm/macrotest/refs/heads/add-license/LICENSE-APACHE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Test harness for macro expansion.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# copy in license texts
cp -p %{SOURCE2} %{SOURCE3} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
