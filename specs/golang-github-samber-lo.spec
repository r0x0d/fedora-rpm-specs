# Generated by go2rpm 1.15.0.post0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/samber/lo
%global goipath         github.com/samber/lo
Version:                1.47.0

%gometa -L -f

%global common_description %{expand:
Lodash-style Go library based on Go 1.18+ Generics.}

%global golicenses      LICENSE
%global godocs          CHANGELOG.md README.md

Name:           golang-github-samber-lo
Release:        %autorelease
Summary:        Lodash-style Go library based on Go 1.18+ Generics

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
