# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate snafu-derive

Name:           rust-snafu-derive
Version:        0.8.5
Release:        %autorelease
Summary:        Ergonomic error handling library

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/snafu-derive
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An ergonomic error handling library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rust_1_61-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rust_1_61-devel %{_description}

This package contains library source intended for building other packages which
use the "rust_1_61" feature of the "%{crate}" crate.

%files       -n %{name}+rust_1_61-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+unstable-provider-api-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+unstable-provider-api-devel %{_description}

This package contains library source intended for building other packages which
use the "unstable-provider-api" feature of the "%{crate}" crate.

%files       -n %{name}+unstable-provider-api-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
