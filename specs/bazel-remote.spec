# Generated by go2rpm 1.19.0
%bcond check 1

# https://github.com/buchgr/bazel-remote
%global goipath         github.com/buchgr/bazel-remote
Version:                2.6.2

%gometa -L -f


Name:           bazel-remote
Release:        %autorelease
Summary:        A remote cache for Bazel

# Generated by go-vendor-tools
License:        Apache-2.0 AND BSD-2-Clause AND BSD-3-Clause AND MIT
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

ExcludeArch:    aarch64 ppc64le s390x

BuildRequires:  go-vendor-tools

%description
bazel-remote is a HTTP/1.1 and gRPC server that is intended to be used as a
remote build cache for REAPI clients like Bazel or as a component of a remote
execution service.

The cache contents are stored in a directory on disk with a maximum cache size,
and bazel-remote will automatically enforce this limit as needed, by deleting
the least recently used files. S3, GCS and experimental Azure blob storage
proxy backends are also supported.

%prep
%goprep -p1
tar -xf %{S:1}

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
%global gomodulesmode GO111MODULE=on
%gobuild -o %{gobuilddir}/bin/bazel-remote .

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
%gotest ./...
%endif

%files -f %{go_vendor_license_filelist}
%doc examples README.md
%{_bindir}/bazel-remote


%changelog
%autochangelog
