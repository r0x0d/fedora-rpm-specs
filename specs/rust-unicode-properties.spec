# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate unicode-properties

Name:           rust-unicode-properties
Version:        0.1.4
Release:        %autorelease
Summary:        Query character Unicode properties according to UAX #44 and UTR #51

License:        (MIT OR Apache-2.0) AND Unicode-3.0
URL:            https://crates.io/crates/unicode-properties
Source:         %{crates_source}
# * License for packaging:
#   https://github.com/unicode-rs/unicode-properties/pull/6
Source10:       https://github.com/unicode-rs/unicode-properties/raw/a6f9361ec3caa9147b6c00dbe407c68e385091fa/LICENSE-UNICODE
# Manually created patch for downstream crate metadata changes
# * https://github.com/unicode-rs/unicode-properties/pull/6
# * License for packaging:
#   https://github.com/unicode-rs/unicode-properties/pull/6
Patch:          unicode-properties-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Query character Unicode properties according to UAX #44 and UTR #51.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/COPYRIGHT
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%license %{crate_instdir}/LICENSE-UNICODE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+emoji-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+emoji-devel %{_description}

This package contains library source intended for building other packages which
use the "emoji" feature of the "%{crate}" crate.

%files       -n %{name}+emoji-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+general-category-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+general-category-devel %{_description}

This package contains library source intended for building other packages which
use the "general-category" feature of the "%{crate}" crate.

%files       -n %{name}+general-category-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp -p '%{SOURCE10}' .
# Remove the shebang line from the non-executable Python script; if this
# shebang *were* used, it would need to be adjusted to avoid /usr/bin/env,
# https://docs.fedoraproject.org/en-US/packaging-guidelines/#_shebang_lines.
sed -r -i '1{/^#!/d}' scripts/unicode.py
# Make *sure* the script is not executable; we include it in the spirit of
# making sure we have the “original source files” for generated code.
chmod -v a-x scripts/unicode.py
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
