# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate nu-protocol

Name:           rust-nu-protocol
Version:        0.96.1
Release:        %autorelease
Summary:        Nushell's internal protocols, including its abstract syntax tree

License:        MIT
URL:            https://crates.io/crates/nu-protocol
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          nu-protocol-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * Update brotli from 5.0 to 7.0: https://github.com/nushell/nushell/pull/13960
Patch:          nu-protocol-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Nushell's internal protocols, including its abstract syntax tree.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+brotli-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+brotli-devel %{_description}

This package contains library source intended for building other packages which
use the "brotli" feature of the "%{crate}" crate.

%files       -n %{name}+brotli-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+plugin-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+plugin-devel %{_description}

This package contains library source intended for building other packages which
use the "plugin" feature of the "%{crate}" crate.

%files       -n %{name}+plugin-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rmp-serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rmp-serde-devel %{_description}

This package contains library source intended for building other packages which
use the "rmp-serde" feature of the "%{crate}" crate.

%files       -n %{name}+rmp-serde-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * all but test_signature_same_*: these tests don't work from outside the
#   multi-crate project checkout
# * test_signature_same_* did not panic as expected
%cargo_test -- -- --skip config_ --skip fancy_default_errors --skip filesize_format_auto_metric_ --skip filesize_metric_ --skip narratable_errors --skip plugins --skip test_signature_same_name --skip test_signature_same_short
%endif

%changelog
%autochangelog
