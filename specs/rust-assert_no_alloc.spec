# Generated by rust2rpm 28
# * The entire test suite works only in debug mode; this is documented in
#   tests/test.rs and guarded with a compile_error!().
%bcond check 0
%global debug_package %{nil}

%global crate assert_no_alloc

Name:           rust-assert_no_alloc
Version:        1.1.2
Release:        %autorelease
Summary:        Custom Rust allocator allowing to temporarily disable memory

License:        BSD-1-Clause
URL:            https://crates.io/crates/assert_no_alloc
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Custom Rust allocator allowing to temporarily disable memory
(de)allocations for a thread. Aborts or prints a warning if allocating
although forbidden.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+disable_release-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+disable_release-devel %{_description}

This package contains library source intended for building other packages which
use the "disable_release" feature of the "%{crate}" crate.

%files       -n %{name}+disable_release-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+warn_debug-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+warn_debug-devel %{_description}

This package contains library source intended for building other packages which
use the "warn_debug" feature of the "%{crate}" crate.

%files       -n %{name}+warn_debug-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+warn_release-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+warn_release-devel %{_description}

This package contains library source intended for building other packages which
use the "warn_release" feature of the "%{crate}" crate.

%files       -n %{name}+warn_release-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
