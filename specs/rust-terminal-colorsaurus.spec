# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate terminal-colorsaurus

Name:           rust-terminal-colorsaurus
Version:        0.4.7
Release:        %autorelease
Summary:        Determines the background and foreground color of the terminal

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/terminal-colorsaurus
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          terminal-colorsaurus-fix-metadata-auto.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Determines the background and foreground color of the terminal.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/license-apache.txt
%license %{crate_instdir}/license-mit.txt
%doc %{crate_instdir}/readme.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+anstyle-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+anstyle-devel %{_description}

This package contains library source intended for building other packages which
use the "anstyle" feature of the "%{crate}" crate.

%files       -n %{name}+anstyle-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rgb-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rgb-devel %{_description}

This package contains library source intended for building other packages which
use the "rgb" feature of the "%{crate}" crate.

%files       -n %{name}+rgb-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
