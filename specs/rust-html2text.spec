# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate html2text

Name:           rust-html2text
Version:        0.6.0
Release:        %autorelease
Summary:        Render HTML as plain text

License:        MIT
URL:            https://crates.io/crates/html2text
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * drop example-only argparse and termion v1 dev-dependencies
Patch:          html2text-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Render HTML as plain text.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ansi_colours-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ansi_colours-devel %{_description}

This package contains library source intended for building other packages which
use the "ansi_colours" feature of the "%{crate}" crate.

%files       -n %{name}+ansi_colours-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+backtrace-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+backtrace-devel %{_description}

This package contains library source intended for building other packages which
use the "backtrace" feature of the "%{crate}" crate.

%files       -n %{name}+backtrace-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+html_trace-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+html_trace-devel %{_description}

This package contains library source intended for building other packages which
use the "html_trace" feature of the "%{crate}" crate.

%files       -n %{name}+html_trace-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+html_trace_bt-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+html_trace_bt-devel %{_description}

This package contains library source intended for building other packages which
use the "html_trace_bt" feature of the "%{crate}" crate.

%files       -n %{name}+html_trace_bt-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# drop unused example programs that pull in outdated dependencies
rm -rv examples/

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
