# Generated by go2rpm 1.14.0
%bcond check 0
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/parnurzeal/gorequest
%global goipath         github.com/parnurzeal/gorequest
Version:                0.2.16

%gometa -L -f

%global common_description %{expand:
GoRequest -- Simplified HTTP client ( inspired by nodejs SuperAgent ).}

%global golicenses      LICENSE
%global godocs          CHANGELOG CONTRIBUTING.md README.md

Name:           golang-github-parnurzeal-gorequest
Release:        %autorelease
Summary:        GoRequest -- Simplified HTTP client ( inspired by nodejs SuperAgent )

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
