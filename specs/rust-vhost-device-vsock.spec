# Generated by rust2rpm 27
%bcond check 1
%bcond bundled_rust_deps 0%{?rhel:1}

%global crate vhost-device-vsock

Name:           rust-vhost-device-vsock
Version:        0.3.0
Release:        %autorelease
Summary:        Virtio-vsock device using the vhost-user protocol

License:        Apache-2.0 OR BSD-3-Clause
URL:            https://crates.io/crates/vhost-device-vsock
Source0:        %{crates_source}

%if %{with bundled_rust_deps}
# The vendor tarball is created using cargo-vendor-filterer to remove Windows
# related files (https://github.com/cgwalters/cargo-vendor-filterer)
#   spectool -g rust-vhost-device-vsock.spec
#   tar xzf vhost-device-vsock-%%{version}.crate
#   cd vhost-device-vsock-%%{version}
#   cargo vendor-filterer --platform x86_64-unknown-linux-gnu \
#       --platform powerpc64le-unknown-linux-gnu \
#       --platform aarch64-unknown-linux-gnu \
#       --platform riscv64gc-unknown-linux-gnu
#
# Trying to make a reproducible tarball:
#   tar --sort=name \
#       --mtime=2025-09-18 \
#       --owner=0 --group=0 --numeric-owner \
#       -Jcf rust-vhost-device-vsock-%%{version}-vendor.tar.xz vendor
Source1:        %{name}-%{version}-vendor.tar.xz
%endif

# Package dependency vmm-sys-util is only built for these architectures.
ExclusiveArch:  aarch64 ppc64le riscv64 x86_64

%if %{with bundled_rust_deps}
BuildRequires:  rust-toolset
%else
BuildRequires:  cargo-rpm-macros >= 24
%endif

%global _description %{expand:
A virtio-vsock device using the vhost-user protocol.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0
# Apache-2.0 AND BSD-3-Clause
# Apache-2.0 OR BSD-3-Clause
# Apache-2.0 OR BSL-1.0
# Apache-2.0 OR MIT
# BSD-2-Clause OR Apache-2.0 OR MIT
# BSD-3-Clause
# BSD-3-Clause OR Apache-2.0
# MIT
# MIT OR Apache-2.0
# MPL-2.0
# Unlicense OR MIT
# Zlib OR Apache-2.0 OR MIT
License:        Apache-2.0 AND (Apache-2.0 OR BSD-2-Clause OR MIT) AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 OR MIT OR Zlib) AND BSD-3-Clause AND MIT AND (MIT OR Unlicense) AND MPL-2.0 AND Unicode-DFS-2016
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE-APACHE
%license LICENSE-BSD-3-Clause
%license LICENSE.dependencies
%doc CHANGELOG.md
%doc README.md
%{_bindir}/vhost-device-vsock
%{?with_bundled_rust_deps:%license cargo-vendor.txt}
%{_mandir}/man1/%{crate}.1*

%prep
%autosetup -n %{crate}-%{version} -p1 %{?with_bundled_rust_deps:-a1}
%cargo_prep %{?with_bundled_rust_deps:-v vendor}

%if %{without bundled_rust_deps}
%generate_buildrequires
%cargo_generate_buildrequires
%endif

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies
%{?with_bundled_rust_deps:%cargo_vendor_manifest}

%install
%cargo_install
install -pm 0644 -Dt %{buildroot}%{_mandir}/man1 %{crate}.1

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
