# Generated by rust2rpm 28
%bcond check 1

%global crate kisd

Name:           rust-kisd
Version:        1.1.0
Release:        %autorelease
Summary:        Kanzi-in-System / DebugUSB UART support for Linux hosts

License:        MIT
URL:            https://crates.io/crates/kisd
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  systemd-rpm-macros

%global _description %{expand:
Kanzi-in-System / DebugUSB UART support for Linux hosts.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
### BEGIN LICENSE SUMMARY ###
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# BSD-2-Clause OR Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
###  END LICENSE SUMMARY  ###
License:        ((MIT OR Apache-2.0) AND Unicode-DFS-2016) AND (Apache-2.0 OR MIT) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND (BSD-2-Clause OR Apache-2.0 OR MIT) AND MIT AND (MIT OR Apache-2.0) AND (Unlicense OR MIT)
# LICENSE.dependencies contains a full license breakdown
Requires:       systemd-udev

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/kisd
%{_udevrulesdir}/85-apple-debugusb.rules

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install
install -Dpm0644 -t %{buildroot}%{_udevrulesdir}/ etc/udev/rules.d/85-apple-debugusb.rules

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
