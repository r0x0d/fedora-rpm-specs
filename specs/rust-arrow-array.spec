# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate arrow-array

Name:           rust-arrow-array
Version:        57.3.0
Release:        %autorelease
Summary:        Array abstractions for Apache Arrow

License:        Apache-2.0 AND MIT
URL:            https://crates.io/crates/arrow-array
Source:         %{crates_source}
# * https://github.com/apache/arrow-rs/pull/9704
# * updated NOTICE.txt
Source10:       https://raw.githubusercontent.com/michel-slm/arrow-rs/refs/heads/fedora-license-audit/NOTICE.txt
# * missing LICENSE-MIT
Source11:       https://raw.githubusercontent.com/michel-slm/arrow-rs/refs/heads/fedora-license-audit/arrow-array/LICENSE-MIT
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          arrow-array-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * drop criterion, only used for benchmarking
# * update license field
Patch:          arrow-array-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Array abstractions for Apache Arrow.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-MIT
%license %{crate_instdir}/LICENSE.txt
%license %{crate_instdir}/NOTICE.txt
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+chrono-tz-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+chrono-tz-devel %{_description}

This package contains library source intended for building other packages which
use the "chrono-tz" feature of the "%{crate}" crate.

%files       -n %{name}+chrono-tz-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ffi-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ffi-devel %{_description}

This package contains library source intended for building other packages which
use the "ffi" feature of the "%{crate}" crate.

%files       -n %{name}+ffi-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+force_validate-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+force_validate-devel %{_description}

This package contains library source intended for building other packages which
use the "force_validate" feature of the "%{crate}" crate.

%files       -n %{name}+force_validate-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -p %{SOURCE10} %{SOURCE11} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
