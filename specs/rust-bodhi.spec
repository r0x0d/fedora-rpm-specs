# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate bodhi

Name:           rust-bodhi
Version:        2.2.0
Release:        %autorelease
Summary:        Bodhi REST API client

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/bodhi
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Bodhi REST API client.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+data-tests-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+data-tests-devel %{_description}

This package contains library source intended for building other packages which
use the "data-tests" feature of the "%{crate}" crate.

%files       -n %{name}+data-tests-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+offline-tests-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+offline-tests-devel %{_description}

This package contains library source intended for building other packages which
use the "offline-tests" feature of the "%{crate}" crate.

%files       -n %{name}+offline-tests-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+online-tests-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+online-tests-devel %{_description}

This package contains library source intended for building other packages which
use the "online-tests" feature of the "%{crate}" crate.

%files       -n %{name}+online-tests-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# * remove example binaries: they take a long time to build
rm -r examples/

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * skip doctests: they take a long time and tons of memory to build
%cargo_test -- --lib
%endif

%changelog
%autochangelog
