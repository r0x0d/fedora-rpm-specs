# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate http-serde

Name:           rust-http-serde
Version:        2.1.1
Release:        %autorelease
Summary:        Serde support for the http crate

License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/http-serde
Source:         %{crates_source}
# * Add license files
Patch2:         https://gitlab.com/kornelski/http-serde/-/merge_requests/6.patch#/http-serde-2.1.1-license-files.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Serde support for the http crate. (De)serialize HeaderMap, Uri, Method,
StatusCode.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
