# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate vsock

Name:           rust-vsock
Version:        0.5.1
Release:        %autorelease
Summary:        Virtio socket support for Rust

License:        Apache-2.0
URL:            https://crates.io/crates/vsock
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Virtio socket support for Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# These tests test_get_local_cid and test_listener_local_addr are skipped because they
# require a host with vsock kernel modules loaded. This is not available in the build environment.
# The following tests test_stream_addresses test_vsock are skipped because they require a VM
# to be running to establish a vsock connection with a server.
%cargo_test -- -- --skip test_get_local_cid --skip test_listener_local_addr --skip test_stream_addresses --skip test_vsock
%endif

%changelog
%autochangelog
