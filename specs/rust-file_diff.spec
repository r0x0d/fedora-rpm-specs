# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate file_diff

Name:           rust-file_diff
Version:        1.0.0
Release:        %autorelease
Summary:        Atomic utility for diffing files in testing

License:        BSD-3-Clause
URL:            https://crates.io/crates/file_diff
Source:         %{crates_source}
# * license file not in repo: https://github.com/ethanpailes/file_diff-rs/pull/2
Source10:       https://raw.githubusercontent.com/michel-slm/rust-file_diff/refs/heads/add-license/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
An atomic utility for diffing files in testing.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -p %{SOURCE10} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
