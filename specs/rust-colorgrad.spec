# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate colorgrad

Name:           rust-colorgrad
Version:        0.8.0
Release:        %autorelease
Summary:        Color scales library

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/colorgrad
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Remove benchmark-only criterion dev-dependency
Patch:          colorgrad-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Color scales library for data visualization, charts, games, generative
art and others.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/PRESET.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ggr-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ggr-devel %{_description}

This package contains library source intended for building other packages which
use the "ggr" feature of the "%{crate}" crate.

%files       -n %{name}+ggr-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+lab-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+lab-devel %{_description}

This package contains library source intended for building other packages which
use the "lab" feature of the "%{crate}" crate.

%files       -n %{name}+lab-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+named-colors-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+named-colors-devel %{_description}

This package contains library source intended for building other packages which
use the "named-colors" feature of the "%{crate}" crate.

%files       -n %{name}+named-colors-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+preset-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+preset-devel %{_description}

This package contains library source intended for building other packages which
use the "preset" feature of the "%{crate}" crate.

%files       -n %{name}+preset-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+std-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+std-devel %{_description}

This package contains library source intended for building other packages which
use the "std" feature of the "%{crate}" crate.

%files       -n %{name}+std-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
