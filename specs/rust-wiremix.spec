# Generated by rust2rpm 27
%bcond check 1

# prevent library files from being installed
%global cargo_install_lib 0

%global crate wiremix

Name:           rust-wiremix
Version:        0.7.0
Release:        %autorelease
Summary:        TUI mixer for PipeWire

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/wiremix
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Missing dependency crossterm 0.29.0 but works with 0.28.0.
# * Build-only depencency not needed ro run wiremix.
Patch:          wiremix-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 26
BuildRequires:  help2man

%global _description %{expand:
A TUI mixer for PipeWire.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# (MIT OR Apache-2.0) AND Unicode-DFS-2016
# Apache-2.0 OR BSL-1.0
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
# Zlib
License:        %{shrink:
    (MIT OR Apache-2.0) AND
    MIT AND
    Unicode-DFS-2016 AND
    Zlib AND
    (Apache-2.0 OR BSL-1.0) AND
    (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND
    (Unlicense OR MIT)
    }
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE-APACHE
%license LICENSE-MIT
%license LICENSE.dependencies
%doc README.md
%doc wiremix.toml
%{_bindir}/wiremix
%{_mandir}/man1/wiremix.1*

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies
help2man --no-info --name='%{summary}' --output=wiremix.1 target/rpm/wiremix

%install
%cargo_install
install -t %{buildroot}%{_mandir}/man1 -D -p -m 0644 wiremix.1

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
