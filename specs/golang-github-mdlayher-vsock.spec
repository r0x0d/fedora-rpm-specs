# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0
%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/mdlayher/vsock
%global goipath         github.com/mdlayher/vsock
Version:                1.2.1

%gometa -L -f

%global common_description %{expand:
Package vsock provides access to Linux VM sockets (AF_VSOCK) for communication
between a hypervisor and its virtual machines.}

%global golicenses      LICENSE.md
%global godocs          CHANGELOG.md README.md

Name:           golang-github-mdlayher-vsock
Release:        %autorelease
Summary:        Provides access to Linux VM sockets (AF_VSOCK)

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
