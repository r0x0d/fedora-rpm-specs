# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate tree-sitter-elm

Name:           rust-tree-sitter-elm
Version:        5.9.0
Release:        %autorelease
Summary:        Elm grammar for tree-sitter

License:        MIT
URL:            https://crates.io/crates/tree-sitter-elm
Source:         %{crates_source}
# * LICENSE.md file not shipped, see
#   https://github.com/elm-tooling/tree-sitter-elm/pull/201
Source1:        https://raw.githubusercontent.com/elm-tooling/tree-sitter-elm/refs/tags/v%{version}/LICENSE.md

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Elm grammar for tree-sitter.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp -p %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install
cp -p LICENSE.md %{buildroot}%{crate_instdir}/

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
