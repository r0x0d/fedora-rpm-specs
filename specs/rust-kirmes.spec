# Generated by rust2rpm 28
%bcond check 1

# MANUALLY ADDED START
%define kirmes_so_version 0

# Apache-2.0 OR BSL-1.0
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# LGPL-2.1-or-later
# MIT
# MIT OR Apache-2.0
# Unlicense OR MIT
%global binary_license LGPL-2.1-or-later AND MIT AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 OR MIT) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND (Unlicense OR MIT)
# LICENSE.dependencies contains a full license breakdown
# MANUALLY ADDED END

%global crate kirmes

Name:           rust-kirmes
Version:        0.1.0
Release:        %autorelease
Summary:        Library providing access to systemd's userdb varlink interface

License:        LGPL-2.1-or-later
URL:            https://crates.io/crates/kirmes
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  cargo-c
BuildRequires:  gcc
BuildRequires:  rustfmt

%global _description %{expand:
A library providing access to systemd's userdb varlink interface.}

%description %{_description}

%package     -n kirmesctl
Summary:        %{summary}
License:        %{binary_license}
# LICENSE.dependencies contains a full license breakdown

%description -n kirmesctl %{_description}

%files       -n kirmesctl
%license LICENSE
%license LICENSE.dependencies
%doc CHANGELOG.md
%doc README.md
%{_bindir}/kirmesctl

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+macro_rules_attribute-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+macro_rules_attribute-devel %{_description}

This package contains library source intended for building other packages which
use the "macro_rules_attribute" feature of the "%{crate}" crate.

%files       -n %{name}+macro_rules_attribute-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+smol-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+smol-devel %{_description}

This package contains library source intended for building other packages which
use the "smol" feature of the "%{crate}" crate.

%files       -n %{name}+smol-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+smol-macros-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+smol-macros-devel %{_description}

This package contains library source intended for building other packages which
use the "smol-macros" feature of the "%{crate}" crate.

%files       -n %{name}+smol-macros-devel
%ghost %{crate_instdir}/Cargo.toml

# MANUALLY ADDED START
%package     -n lib%{crate}
Summary:        %{summary}
License:        %{binary_license}

%description -n lib%{crate}
A shared library providing access to system's userdb varlink interface.

%files       -n lib%{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_libdir}/libkirmes.so.%{kirmes_so_version}*

%package     -n lib%{crate}-devel
Summary:        Development files for libkirmes
License:        LGPL-2.1-or-later
Requires:       lib%{crate}%{?_isa} = %{version}-%{release}

%description -n lib%{crate}-devel
Header files and package config for lib%{crate}.

%files       -n lib%{crate}-devel
%{_libdir}/libkirmes.so
%{_libdir}/pkgconfig/kirmes.pc
%{_includedir}/kirmes/
# MANUALLY ADDED END

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -f binaries

%build
%cargo_build -f binaries
%{cargo_license_summary -f binaries}
%{cargo_license -f binaries} > LICENSE.dependencies
%cargo_cbuild

%install
%cargo_install -f binaries
%cargo_cinstall

# Remove static library
rm -v %{buildroot}/%{_libdir}/libkirmes.a

%if %{with check}
%check
%cargo_test -f binaries
%endif

%changelog
%autochangelog
