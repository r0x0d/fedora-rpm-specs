# Generated by rust2rpm 25
%bcond_without check
%global debug_package %{nil}

%global crate edit

Name:           rust-edit
Version:        0.1.5
Release:        %autorelease
Summary:        Open a file in the default text editor

License:        CC0-1.0
URL:            https://crates.io/crates/edit
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Open a file in the default text editor.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+better-path-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+better-path-devel %{_description}

This package contains library source intended for building other packages which
use the "better-path" feature of the "%{crate}" crate.

%files       -n %{name}+better-path-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+quoted-env-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+quoted-env-devel %{_description}

This package contains library source intended for building other packages which
use the "quoted-env" feature of the "%{crate}" crate.

%files       -n %{name}+quoted-env-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+shell-words-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+shell-words-devel %{_description}

This package contains library source intended for building other packages which
use the "shell-words" feature of the "%{crate}" crate.

%files       -n %{name}+shell-words-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+which-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+which-devel %{_description}

This package contains library source intended for building other packages which
use the "which" feature of the "%{crate}" crate.

%files       -n %{name}+which-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
