# Generated by go2rpm
%bcond_without check

%global debug_package %{nil}

# https://github.com/hashicorp/raft-mdb
%global goipath         github.com/hashicorp/raft-mdb
%global commit          9ee9663b6ffaec215bb1f71a20bb6018225f9521

%gometa

%global common_description %{expand:
LMDB backend for Raft. The package exports the MDBStore which is an
implementation of both a LogStore and StableStore.}

%global golicenses      LICENSE
%global godocs          README.md

%global gosupfiles      glide.lock glide.yaml

Name:           %{goname}
Version:        0
Release:        %autorelease
Summary:        LMDB backend for Raft

# Upstream license specification: MPL-2.0
License:        MPL-2.0
URL:            %{gourl}
Source0:        %{gosource}
Source1:        glide.yaml
Source2:        glide.lock

BuildRequires:  golang(github.com/armon/gomdb)
BuildRequires:  golang(github.com/hashicorp/go-msgpack/codec)
BuildRequires:  golang(github.com/hashicorp/raft)

%if %{with check}
# Tests
BuildRequires:  golang(github.com/hashicorp/raft/bench)
%endif

%description
%{common_description}

%gopkg

%prep
%goprep
cp %{S:1} %{S:2} .

%install
%gopkginstall

# Remove in F33
# Remove erroneous glide.lock folder
%pretrans devel -p <lua>
path = "%{gopath}/src/%{goipath}/glide.lock"
st = posix.stat(path)
if st and st.type == "directory" then
  os.remove(path)
end

%if %{with check}
%check
%gocheck -d .
%endif

%gopkgfiles

%changelog
%autochangelog
