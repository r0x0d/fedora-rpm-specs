# Generated by rust2rpm 24
# * outdated dev-dependencies: compiletest_rs, nix
# * some tests fail on s390x
# * some tests fail with recent versions of Rust (1.60+)
%bcond_with check
%global debug_package %{nil}

%global crate zmq

Name:           rust-zmq
Version:        0.9.2
Release:        %autorelease
Summary:        High-level bindings to the zeromq library

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/zmq
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * remove feature for statically linking against a vendored copy of libzmq
Patch:          zmq-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
High-level bindings to the zeromq library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/HACKING.md
%doc %{crate_instdir}/NEWS.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+zmq_has-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+zmq_has-devel %{_description}

This package contains library source intended for building other packages which
use the "zmq_has" feature of the "%{crate}" crate.

%files       -n %{name}+zmq_has-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
mv examples/zguide/README.md README-examples-zguide.md
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
