# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate fax

Name:           rust-fax
Version:        0.2.7
Release:        %autorelease
Summary:        Decoder and Encoder for CCITT Group 3 and 4 bi-level image encodings

License:        MIT
URL:            https://crates.io/crates/fax
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Drop tiff dev-dependency to avoid a dependency cycle; it turns out that we
#   are not building any tests that would have required it anyway.
Patch:          fax-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Decoder and Encoder for CCITT Group 3 and 4 bi-level image encodings
used by fax machines TIFF and PDF.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+debug-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+debug-devel %{_description}

This package contains library source intended for building other packages which
use the "debug" feature of the "%{crate}" crate.

%files       -n %{name}+debug-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+generate_bitmaps-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+generate_bitmaps-devel %{_description}

This package contains library source intended for building other packages which
use the "generate_bitmaps" feature of the "%{crate}" crate.

%files       -n %{name}+generate_bitmaps-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# * Integration tests require a fax-tests/ directory with sample files, which is
#   not even included in the git repository, let alone in the crate.
%cargo_test -- --doc
%cargo_test -- --lib
%endif

%changelog
%autochangelog
