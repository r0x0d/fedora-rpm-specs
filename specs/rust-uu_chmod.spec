# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

# prevent executables from being installed
%global cargo_install_bin 0

%global crate uu_chmod

Name:           rust-uu_chmod
Version:        0.7.0
Release:        %autorelease
Summary:        chmod ~ (uutils) change mode of FILE

License:        MIT
URL:            https://crates.io/crates/uu_chmod
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * cargo2rpm does not unify multiple uucore dependencies split across targets
#   https://codeberg.org/rust2rpm/cargo2rpm/issues/26
Patch:          uu_chmod-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 26

%global _description %{expand:
chmod ~ (uutils) change mode of FILE.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.package.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
