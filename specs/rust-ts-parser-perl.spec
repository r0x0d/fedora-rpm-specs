# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate ts-parser-perl

Name:           rust-ts-parser-perl
Version:        1.2.0
Release:        %autorelease
Summary:        Actively maintained tree-sitter grammar for Perl

License:        MIT
URL:            https://crates.io/crates/ts-parser-perl
Source:         %{crates_source}
# * add license: https://github.com/tree-sitter-perl/tree-sitter-perl/pull/260
Source2:        https://raw.githubusercontent.com/tree-sitter-perl/tree-sitter-perl/refs/tags/v%{version}/LICENSE
# Manually created patch for downstream crate metadata changes
# * include LICENSE
Patch:          ts-parser-perl-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Actively maintained tree-sitter grammar for Perl.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# copy in license text
cp -p %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
