# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate av-decoders

Name:           rust-av-decoders
Version:        0.11.1
Release:        %autorelease
Summary:        Decoders for use in the rust-av ecosystem

License:        MIT
URL:            https://crates.io/crates/av-decoders
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Remove benchmark-only criterion dev-dependency
# * Remove features for building ffmpeg and ffms2 and linking them statically
# * Remove ffmpeg feature (rust-ffmpeg-the-third not packaged)
# * Remove ffms2 feature (rust-ffms2-sys not packaged)
# * Remove vapoursynth feature (rust-vapoursynth not packaged)
Patch:          av-decoders-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Decoders for use in the rust-av ecosystem.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
