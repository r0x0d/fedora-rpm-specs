# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/andreaskoch/go-fswatch
%global goipath         github.com/andreaskoch/go-fswatch
Version:                1.0.0

%gometa -L -f

%global common_description %{expand:
Fswatch is a go library for monitoring filesystem changes that does not depend
on inotify.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-andreaskoch-fswatch
Release:        %autorelease
Summary:        Fswatch is a go library for monitoring filesystem changes

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
