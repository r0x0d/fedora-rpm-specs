Name:           perl-Crypt-SysRandom-XS
Version:        0.011
Release:        1%{?dist}
Summary:        Perl interface to system randomness, XS version
License:        GPL-1.0-or-later OR Artistic-1.0-Perl
URL:            https://metacpan.org/dist/Crypt-SysRandom-XS
Source0:        https://cpan.metacpan.org/authors/id/L/LE/LEONT/Crypt-SysRandom-XS-%{version}.tar.gz
BuildRequires:  coreutils
BuildRequires:  findutils
BuildRequires:  gcc
BuildRequires:  perl-devel
BuildRequires:  perl-generators
BuildRequires:  perl-interpreter
BuildRequires:  perl(:VERSION) >= 5.10.0
BuildRequires:  perl(Dist::Build) >= 0.025
BuildRequires:  perl(strict)
BuildRequires:  perl(warnings)
# Run-time
BuildRequires:  perl(Exporter)
BuildRequires:  perl(XSLoader)
# Tests
BuildRequires:  perl(blib)
BuildRequires:  perl(File::Spec)
BuildRequires:  perl(IO::Handle)
BuildRequires:  perl(IPC::Open3)
BuildRequires:  perl(Test::More)

%description
This module uses whatever C interface is available to procure
cryptographically random data from the system.

%package tests
Summary:        Tests for %{name}
BuildArch:      noarch
Requires:       %{name} = %{?epoch:%{epoch}:}%{version}-%{release}
Requires:       perl-Test-Harness

%description tests
Tests from %{name}. Execute them
with "%{_libexecdir}/%{name}/test".

%prep
%setup -q -n Crypt-SysRandom-XS-%{version}
# Help generators to recognize Perl scripts
for F in t/*.t; do
    perl -i -MConfig -ple 'print $Config{startperl} if $. == 1 && !s{\A#!.*perl\b}{$Config{startperl}}' "$F"
    chmod +x "$F"
done

%build
perl Build.PL --installdirs=vendor optimize="$RPM_OPT_FLAGS"
./Build

%install
./Build install --destdir=$RPM_BUILD_ROOT --create_packlist=0
find $RPM_BUILD_ROOT -type f -name '*.bs' -size 0 -delete
%{_fixperms} $RPM_BUILD_ROOT/*

# Install tests
mkdir -p %{buildroot}%{_libexecdir}/%{name}
cp -a t %{buildroot}%{_libexecdir}/%{name}
cat > %{buildroot}%{_libexecdir}/%{name}/test << 'EOF'
#!/bin/sh
cd %{_libexecdir}/%{name} && exec prove -I . -j "$(getconf _NPROCESSORS_ONLN)"
EOF
chmod +x %{buildroot}%{_libexecdir}/%{name}/test

%check
./Build test

%files
%doc Changes README SECURITY.md
%license LICENSE
%{perl_vendorarch}/auto/Crypt*
%{perl_vendorarch}/Crypt*
%{_mandir}/man3/Crypt::SysRandom*

%files tests
%{_libexecdir}/%{name}

%changelog
* Wed Feb 18 2026 Jitka Plesnikova <jplesnik@redhat.com> - 0.011-1
- 0.011 bump (rhbz#2440318)

* Sat Jan 17 2026 Fedora Release Engineering <releng@fedoraproject.org> - 0.009-3
- Rebuilt for https://fedoraproject.org/wiki/Fedora_44_Mass_Rebuild

* Fri Aug 29 2025 Jitka Plesnikova <jplesnik@redhat.com> - 0.009-2
- Package tests

* Mon Aug 11 2025 Jitka Plesnikova <jplesnik@redhat.com> - 0.009-1
- Specfile autogenerated by cpanspec 1.78.
