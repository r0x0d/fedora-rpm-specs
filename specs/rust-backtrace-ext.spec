# Generated by rust2rpm 25
# * resolve dependency loop with miette
%bcond_with check
%global debug_package %{nil}

%global crate backtrace-ext

Name:           rust-backtrace-ext
Version:        0.2.1
Release:        %autorelease
Summary:        Minor conveniences on top of the backtrace crate

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/backtrace-ext
Source:         %{crates_source}
# https://github.com/Gankra/backtrace-ext/issues/1
Source:         https://github.com/Gankra/backtrace-ext/raw/fddf332/LICENSE-APACHE
Source:         https://github.com/Gankra/backtrace-ext/raw/fddf332/LICENSE-MIT

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  dos2unix

%global _description %{expand:
Minor conveniences on top of the backtrace crate.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE1} %{SOURCE2} .
dos2unix README.md

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
