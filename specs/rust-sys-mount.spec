# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate sys-mount

Name:           rust-sys-mount
Version:        3.1.0
Release:        %autorelease
Summary:        High level FFI binding around the sys mount & umount2 calls

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/sys-mount
Source:         %{crates_source}
Source2:        https://www.apache.org/licenses/LICENSE-2.0.txt

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
High level FFI binding around the sys mount & umount2 calls.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/LICENSE-Apache-2.0.txt
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+loop-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+loop-devel %{_description}

This package contains library source intended for building other packages which
use the "loop" feature of the "%{crate}" crate.

%files       -n %{name}+loop-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+loopdev-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+loopdev-devel %{_description}

This package contains library source intended for building other packages which
use the "loopdev" feature of the "%{crate}" crate.

%files       -n %{name}+loopdev-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
cp %{SOURCE2} LICENSE-Apache-2.0.txt
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
