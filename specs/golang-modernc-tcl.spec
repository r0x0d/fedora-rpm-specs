# Generated by go2rpm 1.9.0
%bcond_without check
%global debug_package %{nil}

# https://gitlab.com/cznic/tcl
%global goipath         modernc.org/tcl
%global forgeurl        https://gitlab.com/cznic/tcl
Version:                1.15.3
%global tag             v%{version}

%gometa -f

%global common_description %{expand:
Tcl (Tool Command Language) for Go without cgo.}

%global golicenses      LICENSE license.terms assets/license.terms
%global godocs          AUTHORS CONTRIBUTORS README.md

Name:           %{goname}
Release:        %autorelease
Summary:        Tcl (Tool Command Language) for Go without cgo.}

License:        TCL AND BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep
%autopatch -p1

%generate_buildrequires
%go_generate_buildrequires

%build
%gobuild -o %{gobuilddir}/bin/tcl %{goipath}

%install
%gopkginstall
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%if %{with check}
%check
# .: test fails on Rawhide.
# lib: ./tcl_linux_amd64.go:272125:6: redundant or: *(*int32)(unsafe.Pointer(bp)) == EAGAIN || *(*int32)(unsafe.Pointer(bp)) == EAGAIN
%gocheck -d . -d lib
%endif

%files
%license LICENSE license.terms assets/license.terms
%doc AUTHORS CONTRIBUTORS README.md
%{_bindir}/tcl

%gopkgfiles

%changelog
%autochangelog
