# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate decimal

Name:           rust-decimal
Version:        2.1.0
Release:        %autorelease
Summary:        Floating point arithmetic for Rust

# decimal crate:        Apache-2.0
# bundled decNumber:    ICU
License:        Apache-2.0 AND ICU
URL:            https://crates.io/crates/decimal
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * prevent test binary from being built and shipped
# * update license in crate metadata to reflect bundled decNumber library
Patch:          decimal-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Decimal floating point arithmetic for Rust.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch
Provides:       bundled(decNumber)

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%license %{crate_instdir}/decNumber/ICU-license.html
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+ord_subset-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+ord_subset-devel %{_description}

This package contains library source intended for building other packages which
use the "ord_subset" feature of the "%{crate}" crate.

%files       -n %{name}+ord_subset-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rustc-serialize-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rustc-serialize-devel %{_description}

This package contains library source intended for building other packages which
use the "rustc-serialize" feature of the "%{crate}" crate.

%files       -n %{name}+rustc-serialize-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+serde-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+serde-devel %{_description}

This package contains library source intended for building other packages which
use the "serde" feature of the "%{crate}" crate.

%files       -n %{name}+serde-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
