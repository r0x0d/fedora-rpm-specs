# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate mdbook-renderer

Name:           rust-mdbook-renderer
Version:        0.5.3
Release:        %autorelease
Summary:        Library to assist implementing an mdBook renderer

License:        MPL-2.0
URL:            https://crates.io/crates/mdbook-renderer
Source:         %{crates_source}
# * https://github.com/rust-lang/mdBook/pull/3135
Source2:        https://github.com/rust-lang/mdBook/raw/refs/tags/v0.5.3/LICENSE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Library to assist implementing an mdBook renderer.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
