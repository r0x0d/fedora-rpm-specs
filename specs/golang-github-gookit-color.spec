# Generated by go2rpm 1.15.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/gookit/color
%global goipath         github.com/gookit/color
Version:                1.5.4

%gometa -L -f

%global common_description %{expand:
Terminal color rendering library, supporting 8/16 colors, 256 colors, RGB color
rendering output, supporting Print/Sprintf methods.}

%global golicenses      LICENSE
%global godocs          _examples README.md README.zh-CN.md

Name:           golang-github-gookit-color
Release:        %autorelease
Summary:        Terminal color rendering library

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
