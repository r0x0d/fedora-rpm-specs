# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate jiter

Name:           rust-jiter
Version:        0.5.0
Release:        %autorelease
Summary:        Fast Iterable JSON parser

License:        MIT
URL:            https://crates.io/crates/jiter
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
BuildRequires:  tomcli

%global _description %{expand:
Fast Iterable JSON parser.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+python-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+python-devel %{_description}

This package contains library source intended for building other packages which
use the "python" feature of the "%{crate}" crate.

%files       -n %{name}+python-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# Omit benchmark-only dependencies.
tomcli-set Cargo.toml del dev-dependencies.bencher
tomcli-set Cargo.toml del dev-dependencies.codspeed-bencher-compat

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
