# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate font-test-data

Name:           rust-font-test-data
Version:        0.7.0
Release:        %autorelease
Summary:        Test data for the fontations crates

License:        (MIT OR Apache-2.0) AND Apache-2.0 AND OFL-1.1 AND Unicode-3.0
URL:            https://crates.io/crates/font-test-data
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Add OFL-1.1, Apache-2.0 and Unicode-3.0 terms to the SPDX license expression
#   to account for fonts in test_data/; see test_data/README.md for an
#   accounting of which licenses apply to which fonts.
Patch:          font-test-data-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Test data for the fontations crates.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
# Do not generate a dependency on the bash shell
chmod a-x test_data/rebuild.sh
# Remove the shebang because we unset the execute bit and because it uses env,
# https://docs.fedoraproject.org/en-US/packaging-guidelines/#_shebang_lines.
sed -r -i '1{/^#!/d}' test_data/rebuild.sh
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
