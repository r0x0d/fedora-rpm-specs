# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/rasky/go-xdr
%global goipath         github.com/rasky/go-xdr
%global commit          1a41d1a06c93bf8a0e0385be3847a277bb793187

%gometa -L -f

%global common_description %{expand:
Implements the XDR standard as specified in RFC 4506 in pure Go.}

%global golicenses      LICENSE
%global godocs          README.md

Name:           golang-github-rasky-xdr
Version:        0
Release:        %autorelease -p
Summary:        Implements the XDR standard as specified in RFC 4506 in pure Go

License:        ISC
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
