# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}

%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/wasilibs/wazero-helpers
%global goipath         github.com/wasilibs/wazero-helpers
%global commit          3dff1577cd52055bf15f051baa027647232025ef

%gometa -L -f

%global common_description %{expand:
Provides higher level utilities to support some of these use cases and hopes
to provide a helpful base for wazero users that fall into these categories,
while allowing wazero itself to continue to focus on being the an excellent
WebAssembly runtime for Go}

%global golicenses      LICENSE
%global godocs          CODE_OF_CONDUCT.md README.md

Name:           golang-github-wasilibs-wazero-helpers
Version:        0
Release:        %autorelease -p
Summary:        Helper libraries for wazero

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

rm -rf build

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
