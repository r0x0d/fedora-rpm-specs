# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate string-cases

Name:           rust-string-cases
Version:        0.2.0
Release:        %autorelease
Summary:        String case conversion utilities

License:        MIT
URL:            https://crates.io/crates/string-cases
Source:         %{crates_source}
# * Add a license file
# * https://github.com/kaleidawave/string-cases/issues/2
# * https://docs.fedoraproject.org/en-US/packaging-guidelines/LicensingGuidelines/#_license_text
# * Text and copyright statement based on
#   https://github.com/rust-lang/rust/raw/refs/tags/1.90.0/LICENSE-MIT, with
#   “The Rust Project Contributors” replaced by “Roland Ben,” as @ModProg
#   appears to be the sole contributor.
Source10:       https://github.com/kaleidawave/string-cases/raw/787ef44c626edc30e1aaf420da5eb14fca0f491a/LICENCE

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
String case conversion utilities.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
# Copy the proposed license file into the source.
cp -p '%{SOURCE10}' ./LICENSE
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
