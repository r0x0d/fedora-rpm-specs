# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/jwalton/go-supportscolor
%global goipath         github.com/jwalton/go-supportscolor
Version:                1.2.0

%gometa -L -f

%global common_description %{expand:
Golang library to detect whether a terminal supports color.}

%global golicenses      LICENSE pkg/hasFlag/LICENSE-hasFlag
%global godocs          README.md

Name:           golang-github-jwalton-supportscolor
Release:        %autorelease
Summary:        Golang library to detect whether a terminal supports color

License:        MIT
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

mv pkg/hasFlag/LICENSE pkg/hasFlag/LICENSE-hasFlag

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
