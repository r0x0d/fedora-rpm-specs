# Generated by go2rpm 1.14.0
%bcond check 1
%bcond bootstrap 0

%global debug_package %{nil}
%if %{with bootstrap}
%global __requires_exclude %{?__requires_exclude:%{__requires_exclude}|}^golang\\(.*\\)$
%endif

# https://github.com/munnerz/goautoneg
%global goipath         github.com/munnerz/goautoneg
%global commit          a7dc8b61c822528f973a5e4e7b272055c6fdb43e

%gometa -L -f

%global common_description %{expand:
HTTP Content-Type Autonegotiation.}

%global golicenses      LICENSE
%global godocs          README.txt

Name:           golang-github-munnerz-goautoneg
Version:        0
Release:        %autorelease -p
Summary:        HTTP Content-Type Autonegotiation

License:        BSD-3-Clause
URL:            %{gourl}
Source:         %{gosource}

%description %{common_description}

%gopkg

%prep
%goprep -A
%autopatch -p1

%if %{without bootstrap}
%generate_buildrequires
%go_generate_buildrequires
%endif

%install
%gopkginstall

%if %{without bootstrap}
%if %{with check}
%check
%gocheck
%endif
%endif

%gopkgfiles

%changelog
%autochangelog
