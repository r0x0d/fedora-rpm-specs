# Generated by go2rpm 1.15.0
%bcond check 1

# https://github.com/golang/perf
%global goipath         golang.org/x/perf
%global forgeurl        https://github.com/golang/perf
%global commit          02a15fd477bac975be19f213ea665ad854766179

%gometa

%global common_description %{expand:
This package holds the source for various tools related to performance
measurement, storage, and analysis.

 - cmd/benchstat contains a command-line tool that computes and 7
 compares statistics about benchmarks.
 - cmd/benchsave contains a command-line tool for publishing benchmark
 results.
 - storage contains the https://perfdata.golang.org/ benchmark result
 storage system.
 - analysis contains the https://perf.golang.org/ benchmark result analysis
 system.}

Name:           golang-x-perf
Version:        0
Release:        %autorelease -p
Summary:        Performance measurement, storage, and analysis

# Generated by go-vendor-tools
License:     	Apache-2.0 AND BSD-3-Clause AND CC-BY-4.0 AND FTL AND MIT AND MPL-2.0 AND OFL-1.1
URL:            %{gourl}
Source0:        %{gosource}
# Generated by go-vendor-tools
Source1:        %{archivename}-vendor.tar.bz2
Source2:        go-vendor-tools.toml

BuildRequires:  go-vendor-tools

%description %{common_description}

%prep
%goprep -A
%setup -q -T -D -a1 %{forgesetupargs}

%generate_buildrequires
%go_vendor_license_buildrequires -c %{S:2}

%build
for cmd in cmd/* ; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done
for cmd in analysis/appengine analysis/localperf benchmath storage/appengine storage/localperfdata; do
  %gobuild -o %{gobuilddir}/bin/$(basename $cmd) %{goipath}/$cmd
done

%install
%go_vendor_license_install -c %{S:2}
install -m 0755 -vd                     %{buildroot}%{_bindir}
install -m 0755 -vp %{gobuilddir}/bin/* %{buildroot}%{_bindir}/

%check
%go_vendor_license_check -c %{S:2}
%if %{with check}
%gocheck
%endif

%files -f %{go_vendor_license_filelist}
%license vendor/modules.txt
%doc CONTRIBUTING.md README.md
%{_bindir}/appengine
%{_bindir}/benchfilter
%{_bindir}/benchmath
%{_bindir}/benchsave
%{_bindir}/benchseries
%{_bindir}/benchstat
%{_bindir}/localperf
%{_bindir}/localperfdata


%changelog
%autochangelog
