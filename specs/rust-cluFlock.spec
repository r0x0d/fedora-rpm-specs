# Generated by rust2rpm 26
%bcond_without check
%global debug_package %{nil}

%global crate cluFlock

Name:           rust-cluFlock
Version:        1.2.7
Release:        %autorelease
Summary:        Installation and subsequent safe removal of flock locks for data streams

# Upstream license specification:  Apache-2.0
License:        Apache-2.0
URL:            https://crates.io/crates/cluFlock
Source:         %{crates_source}
# Automatically generated patch to strip dependencies and normalize metadata
Patch:          cluFlock-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * drop Windows-specific features
Patch:          cluFlock-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Installation and subsequent safe removal of `flock` locks for data
streams.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
# drop executable bits from all source files
find -type f -executable -print -exec chmod -x {} +

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
