# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate nuon

Name:           rust-nuon
Version:        0.99.1
Release:        %autorelease
Summary:        Support for the NUON format

License:        MIT
URL:            https://crates.io/crates/nuon
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Update fancy-regex from 0.13 to 0.16:
#   https://github.com/nushell/nushell/commit/0e3ca7b355bd62f1cfc372b414defd528da04718,
#   https://github.com/nushell/nushell/commit/1e2fa68db0f7e877fe9c4cd95ab16f1c0df793e8
Patch:          nuon-fix-metadata.diff
# * some tests include missing fixtures during compilation
Patch2:         nuon-remove-tests-with-missing-fixtures.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Support for the NUON format.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
