# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate lockfree-object-pool

Name:           rust-lockfree-object-pool
Version:        0.1.6
Release:        %autorelease
Summary:        Thread-safe object pool with automatic return and attach/detach semantics

License:        BSL-1.0
URL:            https://crates.io/crates/lockfree-object-pool
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Drop benchmark-only dev dependencies criterion, criterion-plot, object-pool,
#   and sharded-slab.
Patch:          lockfree-object-pool-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A thread-safe object pool collection with automatic return and
attach/detach semantics.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE_1_0.txt
%doc %{crate_instdir}/LICENSE_1_0.txt
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
