# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate deadpool-runtime

Name:           rust-deadpool-runtime
Version:        0.1.4
Release:        %autorelease
Summary:        Dead simple async pool utilities for sync managers

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/deadpool-runtime
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * Fix a small but prominent typo:
#   https://github.com/deadpool-rs/deadpool/pull/410
Patch:          deadpool-runtime-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Dead simple async pool utilities for sync managers.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/CHANGELOG.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+tokio_1-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+tokio_1-devel %{_description}

This package contains library source intended for building other packages which
use the "tokio_1" feature of the "%{crate}" crate.

%files       -n %{name}+tokio_1-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
