# Generated by rust2rpm 28
%bcond check 1
%global debug_package %{nil}

%global crate vergen-lib

Name:           rust-vergen-lib
Version:        9.1.0
Release:        %autorelease
Summary:        Common code used to support the vergen libraries

License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/vergen-lib
Source:         %{crates_source}
# * Downstream patch: the test_util workspace crate dep. is currently unused
Patch2:         vergen-lib-9.1.0-remove-test-util.patch

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Common code used to support the vergen libraries.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+build-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+build-devel %{_description}

This package contains library source intended for building other packages which
use the "build" feature of the "%{crate}" crate.

%files       -n %{name}+build-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+cargo-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+cargo-devel %{_description}

This package contains library source intended for building other packages which
use the "cargo" feature of the "%{crate}" crate.

%files       -n %{name}+cargo-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+emit_and_set-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+emit_and_set-devel %{_description}

This package contains library source intended for building other packages which
use the "emit_and_set" feature of the "%{crate}" crate.

%files       -n %{name}+emit_and_set-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+git-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+git-devel %{_description}

This package contains library source intended for building other packages which
use the "git" feature of the "%{crate}" crate.

%files       -n %{name}+git-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+rustc-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+rustc-devel %{_description}

This package contains library source intended for building other packages which
use the "rustc" feature of the "%{crate}" crate.

%files       -n %{name}+rustc-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+si-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+si-devel %{_description}

This package contains library source intended for building other packages which
use the "si" feature of the "%{crate}" crate.

%files       -n %{name}+si-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
