# Generated by rust2rpm 28
%bcond check 1

%global crate cargo-vendor-filterer

Name:           rust-cargo-vendor-filterer
Version:        0.5.18
Release:        %autorelease
Summary:        Cargo vendor, but with filtering for platforms and more

License:        Apache-2.0
URL:            https://crates.io/crates/cargo-vendor-filterer
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * remove unwanted vendored-openssl feature
# * allow cargo-lock 0.11: downstream-only for MSRV reasons
Patch:          cargo-vendor-filterer-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
`cargo vendor`, but with filtering for platforms and more.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# 0BSD OR MIT OR Apache-2.0
# Apache-2.0
# Apache-2.0 OR BSL-1.0
# Apache-2.0 OR MIT
# Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT
# MIT
# MIT OR Apache-2.0
# MIT OR Zlib OR Apache-2.0
# Unicode-3.0
# Unlicense OR MIT
License:        %{shrink:
    (0BSD OR MIT OR Apache-2.0) AND
    Apache-2.0 AND
    (Apache-2.0 OR BSL-1.0) AND
    (Apache-2.0 OR MIT) AND
    (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND
    MIT AND
    (MIT OR Zlib OR Apache-2.0)
    (Unlicense OR MIT) AND
    Unicode-3.0
    }
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/cargo-vendor-filterer

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+openssl-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+openssl-devel %{_description}

This package contains library source intended for building other packages which
use the "openssl" feature of the "%{crate}" crate.

%files       -n %{name}+openssl-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+sha-rs-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+sha-rs-devel %{_description}

This package contains library source intended for building other packages which
use the "sha-rs" feature of the "%{crate}" crate.

%files       -n %{name}+sha-rs-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires -f openssl

%build
%cargo_build -f openssl
%{cargo_license_summary -f openssl}
%{cargo_license -f openssl} > LICENSE.dependencies

%install
%cargo_install -f openssl

%if %{with check}
%check
# * some tests download from crates.io
%cargo_test -f openssl -- -- --skip vendor_filterer::
%endif

%changelog
%autochangelog
