# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate pathfinder_simd

Name:           rust-pathfinder_simd
Version:        0.5.2
Release:        %autorelease
Summary:        Simple SIMD library

# Upstream license specification: MIT/Apache-2.0
License:        MIT OR Apache-2.0
URL:            https://crates.io/crates/pathfinder_simd
Source0:        %{crates_source}
Source1:        https://github.com/servo/pathfinder/raw/pathfinder_simd-v0.5.1/LICENSE-APACHE
Source2:        https://github.com/servo/pathfinder/raw/pathfinder_simd-v0.5.1/LICENSE-MIT

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A simple SIMD library.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+pf-no-simd-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+pf-no-simd-devel %{_description}

This package contains library source intended for building other packages which
use the "pf-no-simd" feature of the "%{crate}" crate.

%files       -n %{name}+pf-no-simd-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep
cp %{SOURCE1} %{SOURCE2} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
# this test fails with floating point accuracy issues
%cargo_test -- -- --skip test_f32x4_basic_ops
%endif

%changelog
%autochangelog
