# Generated by rust2rpm 27
%bcond check 1
%global debug_package %{nil}

%global crate matrixcompare-mock

Name:           rust-matrixcompare-mock
Version:        0.1.0
Release:        %autorelease
Summary:        Internal mock data structures for testing of matrixcompare

License:        MIT
URL:            https://crates.io/crates/matrixcompare-mock
Source:         %{crates_source}
# * https://github.com/Andlon/matrixcompare/issues/8
Source10:       https://github.com/Andlon/matrixcompare/raw/57a66bd/LICENSE
# Manually created patch for downstream crate metadata changes
# * Update proptest dev-dependencies to 1.0:
#   https://github.com/Andlon/matrixcompare/pull/12
Patch:          matrixcompare-mock-fix-metadata.diff

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
Internal mock data structures for testing of matrixcompare.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep
cp -pav %{SOURCE10} .

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
