# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate crypto-hash

Name:           rust-crypto-hash
Version:        0.3.4
Release:        %autorelease
Summary:        Wrapper for OS-level cryptographic hash functions

License:        MIT
URL:            https://crates.io/crates/crypto-hash
Source:         %{crates_source}
# Automatically generated patch to strip foreign dependencies
Patch:          crypto-hash-fix-metadata-auto.diff
# Manually created patch for downstream crate metadata changes
# * bump hex from 0.3 to 0.4: https://github.com/malept/crypto-hash/pull/13
Patch:          crypto-hash-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A wrapper for OS-level cryptographic hash functions.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/CONTRIBUTING.md
%doc %{crate_instdir}/NEWS.md
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
