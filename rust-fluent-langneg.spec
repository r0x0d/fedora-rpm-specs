# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate fluent-langneg

Name:           rust-fluent-langneg
Version:        0.13.0
Release:        %autorelease
Summary:        Library for language and locale negotiation

License:        Apache-2.0
URL:            https://crates.io/crates/fluent-langneg
Source0:        %{crates_source}
# https://github.com/projectfluent/fluent-langneg-rs/pull/22
Source1:        https://github.com/projectfluent/fluent-langneg-rs/raw/29007f8/LICENSE
# Manually created patch for downstream crate metadata changes
# * drop unused, benchmark-only criterion dev-dependency to speed up builds
Patch:          fluent-langneg-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
A library for language and locale negotiation.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%package     -n %{name}+cldr-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+cldr-devel %{_description}

This package contains library source intended for building other packages which
use the "cldr" feature of the "%{crate}" crate.

%files       -n %{name}+cldr-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
cp -pav %{SOURCE1} .
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
