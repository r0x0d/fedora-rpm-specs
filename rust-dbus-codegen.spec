# Generated by rust2rpm 24
%bcond_without check
%global debug_package %{nil}

%global crate dbus-codegen

Name:           rust-dbus-codegen
Version:        0.5.0
Release:        %autorelease
Summary:        Binary crate to generate Rust code from XML introspection data

# Upstream license specification: Apache-2.0/MIT
License:        Apache-2.0 OR MIT
URL:            https://crates.io/crates/dbus-codegen
Source:         %{crates_source}
# Manually created patch for downstream crate metadata changes
# * prevent unused dbus-codegen-rust binary from being built and shipped
# * bump xml-rs dependency from 0.3 to 0.8
Patch:          dbus-codegen-fix-metadata.diff

BuildRequires:  rust-packaging >= 21

%global _description %{expand:
Binary crate to generate Rust code from XML introspection data.}

%description %{_description}

%package        devel
Summary:        %{summary}
BuildArch:      noarch

%description    devel %{_description}

This package contains library source intended for building other packages which
use the "%{crate}" crate.

%files          devel
%license %{crate_instdir}/LICENSE-APACHE
%license %{crate_instdir}/LICENSE-MIT
%doc %{crate_instdir}/README.md
%{crate_instdir}/

%package     -n %{name}+default-devel
Summary:        %{summary}
BuildArch:      noarch

%description -n %{name}+default-devel %{_description}

This package contains library source intended for building other packages which
use the "default" feature of the "%{crate}" crate.

%files       -n %{name}+default-devel
%ghost %{crate_instdir}/Cargo.toml

%prep
%autosetup -n %{crate}-%{version_no_tilde} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
